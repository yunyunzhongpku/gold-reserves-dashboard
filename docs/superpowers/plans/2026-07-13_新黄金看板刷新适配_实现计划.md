# 新黄金看板刷新适配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让每周数据刷新在不改变新黄金看板结构和数据口径的前提下稳定完成测试、提交、发布和线上验证。

**Architecture:** 保留现有 `Wind/FRED/CFTC -> build_site.py -> site/index.html -> GitHub Pages` 链路。只修复依赖当前日期和当前滞后数量的测试，并更新 Codex 自动化提示词，使验收围绕新页面结构和动态数据状态。

**Tech Stack:** Python 3、`unittest`、原生 HTML/SVG/JavaScript、GitHub Pages、Codex cron automation

## Global Constraints

- 不修改 `scripts/build_site.py` 的页面生成行为。
- 不改变 Wind/FRED/CFTC 数据源、对账或发布链路。
- SAFE 官方储备继续由 `data/market/official_reserves_manual.csv` 手工维护。
- 不使用 `--no-reconcile`，不跳过或弱化测试。
- 未跟踪的 `docs/pantheon_research_dashboard_notes.md` 不纳入提交。
- 发布前必须验证新页面关键结构和公开 Pages 页面均已更新。

---

### Task 1: 去除刷新测试中的快照依赖

**Files:**
- Modify: `tests/test_build_site.py:418`
- Test: `tests/test_build_site.py`

**Interfaces:**
- Consumes: `build_site.read_dashboard_data(today=...)` 返回的 layer 日期与动态数据质量。
- Produces: 对未来日期防护和动态质量文案的稳定行为断言。

- [ ] **Step 1: 保留当前失败作为红灯证据**

Run: `python3 -m unittest tests.test_build_site.GoldDashboardDataTest.test_uses_latest_workbook_but_excludes_future_dated_excel_rows tests.test_build_site.GoldDashboardDataTest.test_research_details_include_scoring_method_and_limitations -v`

Expected: 固定 `2026-07-06` 和 `stale > 0` 两项失败。

- [ ] **Step 2: 把定位仓位层纳入不晚于 today 的行为断言**

将 `positioning_technical` 加入日期上界循环，删除其固定日期断言：

```python
for layer_id in ["official_reserves", "epu", "gpr", "positioning_technical"]:
    latest_date = _date.fromisoformat(layers[layer_id]["latest"]["date"])
    self.assertLessEqual(latest_date, self.TODAY)
```

- [ ] **Step 3: 允许动态滞后计数为零**

删除 `self.assertGreater(stale, 0)`，继续断言 HTML 精确包含运行时算出的 `f"{stale} 组滞后"`。

- [ ] **Step 4: 运行两个相关测试**

Run: `python3 -m unittest tests.test_build_site.GoldDashboardDataTest.test_uses_latest_workbook_but_excludes_future_dated_excel_rows tests.test_build_site.GoldDashboardDataTest.test_research_details_include_scoring_method_and_limitations -v`

Expected: 2 tests pass。

### Task 2: 验证刷新不会破坏新页面

**Files:**
- Verify: `scripts/build_site.py`
- Verify: `site/index.html`
- Verify: `tests/test_build_site.py`

**Interfaces:**
- Consumes: 当前四个市场 CSV、官方储备手工 CSV 和最新 Excel。
- Produces: 保留新决策页与研究证据链的静态 HTML。

- [ ] **Step 1: 运行完整测试**

Run: `PYTHONPYCACHEPREFIX=/private/tmp/gold-dashboard-pycache python3 -m unittest tests.test_build_site tests.test_refresh_wind_data tests.test_update_data_and_commit -v`

Expected: 106 tests pass。

- [ ] **Step 2: 检查 HTML 关键结构**

Run: `rg -c 'id="gold-price-section"|class="driver-row"|<details class="evidence-unit"|data-evidence-target=' site/index.html`

Expected: 黄金主图存在、6 行驱动存在、7 个证据单元存在、证据下钻链接存在。

- [ ] **Step 3: 检查格式与变更范围**

Run: `git diff --check`

Expected: 无输出，退出码 0；除计划、测试和允许的生成文件外没有 tracked 改动。

### Task 3: 更新并执行每周自动化

**Files:**
- Modify externally: Codex automation `commit`
- Run: `scripts/update_data_and_commit.py`

**Interfaces:**
- Consumes: 自动化 ID `commit`、当前 `main` 分支和本地 Wind 连接。
- Produces: 数据 commit、GitHub push、Pages deployment 与公开页面核验结果。

- [ ] **Step 1: 更新自动化提示词**

保留每周一 10:00、local execution、模型与状态，只修正文案中的允许文件数量、SAFE 手工边界和新页面结构验收要求。

- [ ] **Step 2: 提交计划与测试修复**

Run: `git add -- docs/superpowers/plans/2026-07-13_新黄金看板刷新适配_实现计划.md tests/test_build_site.py`

Run: `git commit -m "test: stabilize refreshed dashboard checks" -m "Co-Authored-By: Codex <noreply@openai.com>"`

Expected: 工作区只剩四个市场 CSV、`site/index.html` 与未跟踪笔记。

- [ ] **Step 3: 恢复刷新脚本要求的干净 tracked 工作区**

先提交本轮已经刷新并验证过的五个生成文件，或在确认数据需重新抓取时保留它们并由等价的完整流程处理；不得删除用户文件或绕过 clean-worktree 检查。

- [ ] **Step 4: 执行完整刷新发布并验证 Pages**

Run: `python3 scripts/update_data_and_commit.py`

Expected: 刷新、构建、106 项测试、`git diff --check`、生成文件 commit 和 push 全部成功；随后 GitHub Pages workflow 成功，公开页面的构建时间或关键数据日期匹配本次 commit。
