# 黄金决策页实现计划

> **供执行代理：** 必须按任务逐项执行，推荐使用 `superpowers:subagent-driven-development`；也可使用 `superpowers:executing-plans`。所有步骤使用复选框跟踪。

**目标：** 把现有黄金看板改成决策优先的静态页面，修正中国央行购金序列，增加严格均线判定和原生交互，同时把研究检验折叠保留。

**架构：** Python 继续负责数据读取、校验、状态计算和 SVG 几何；生成的 HTML 内嵌全部数据和原生 JavaScript，JavaScript 只切换视图和 tooltip，不重新计算指标。现有本地刷新、提交 CSV、生成 `site/index.html`、GitHub Pages 发布的边界保持不变。

**技术栈：** Python 3 标准库、`openpyxl`、`unittest`、静态 HTML/CSS、原生 SVG、原生 JavaScript。

## 全局约束

- 实现依据：`docs/superpowers/specs/2026-07-11_黄金决策页_设计稿.md`。
- 执行开始时先使用 `superpowers:using-git-worktrees` 创建隔离工作区；分支名使用 `codex/gold-decision-dashboard`。
- 不安装依赖，不引入 React、Chart.js、ECharts 或其它前端库。
- Python 是所有状态、阈值、均线和图表坐标的唯一计算源；JavaScript 不复制业务算法。
- 中国央行储备以国家外汇管理局公布的“万盎司”为原始值，换算常数固定为 `0.311034768 吨/万盎司`。
- 首页姿态只使用实际利率、美元、通胀预期、官方购金、资金与仓位五组驱动。
- `price_trend`、`epu`、`gpr` 保留在研究数据中，但不参与首页姿态。
- `missing` 和 `very-stale` 不进入姿态；有效驱动少于 3 组时显示“数据不足”。
- 最近变化固定为宏观、官方购金、资金与仓位三条。
- 保留现有关系检验和前瞻收益守护测试，不弱化或删除。
- 不触碰未跟踪文件 `docs/pantheon_research_dashboard_notes.md`。
- 本计划允许本地 commit；不执行 `git push`、发布或部署。

---

## 文件结构与职责

- 修改：`data/market/official_reserves_manual.csv`——保存国家外汇管理局中国黄金储备原始值。
- 修改：`scripts/build_site.py`——新增储备校验/合并、技术状态、五组驱动、页面渲染和交互资产。
- 修改：`tests/test_build_site.py`——覆盖所有新口径、回归和 HTML 合同。
- 生成：`site/index.html`——最终静态产物，只在全部实现和浏览器验收后更新并提交。
- 不修改：`scripts/refresh_wind_data.py`、`scripts/refresh_market_data.py`、`scripts/update_data_and_commit.py`——现有刷新和允许提交路径已经覆盖本轮文件。

任务 1—5 都会修改 `scripts/build_site.py`，必须顺序执行；每个任务使用新的执行 subagent，并在下一个任务开始前完成规格复核与代码质量复核。任务 6 使用独立验收 subagent，只读检查后由主代理运行最终命令。

---

### 任务 1：修正并校验中国官方黄金储备序列

**文件：**

- 修改：`data/market/official_reserves_manual.csv`
- 修改：`scripts/build_site.py:11,151-195,633-703,1269-1331`
- 测试：`tests/test_build_site.py:1-52`

**接口：**

- 产出：`read_official_reserve_rows(path: Path, max_date: Optional[date]) -> list[dict]`；实现保持项目现有的无注解函数风格，不要求新增 `typing` 导入。
- 产出：`merge_reserve_rows(base_rows: list[dict], override_rows: list[dict]) -> list[dict]`
- 产出行字段：`date`、`_date`、`china_reserves_10k_oz`、`china_reserves`、`global_reserves`、`china_source`、`global_source`
- 后续依赖：`make_reserve_layer()` 和 `read_dashboard_data()` 只使用合并后的储备行。

- [ ] **步骤 1：先写官方序列、失败条件和字段级合并测试**

在 `tests/test_build_site.py` 顶部增加 `tempfile`，并加入以下测试类：

```python
import tempfile


class OfficialReserveOverrideTest(unittest.TestCase):
    def setUp(self):
        self.tempdir = tempfile.TemporaryDirectory()
        self.path = Path(self.tempdir.name) / "official.csv"

    def tearDown(self):
        self.tempdir.cleanup()

    def write_csv(self, rows):
        header = "date,china_reserves_10k_oz,global_reserves,source\n"
        self.path.write_text(header + "\n".join(rows) + "\n", encoding="utf-8")
        return self.path

    def test_reads_complete_safe_series_and_converts_tonnes(self):
        path = self.write_csv([
            "2025-12-31,7415,,SAFE: 官方储备资产（2025）",
            "2026-01-31,7419,,SAFE: 官方储备资产（2026）",
            "2026-02-28,7422,,SAFE: 官方储备资产（2026）",
            "2026-03-31,7438,,SAFE: 官方储备资产（2026）",
            "2026-04-30,7464,,SAFE: 官方储备资产（2026）",
            "2026-05-31,7496,,SAFE: 官方储备资产（2026）",
            "2026-06-30,7544,,SAFE: 官方储备资产（2026）",
        ])
        rows = build_site.read_official_reserve_rows(path, max_date=_date(2026, 7, 7))
        self.assertEqual(rows[0]["date"], "2025-12-31")
        self.assertAlmostEqual(rows[-2]["china_reserves"], 2331.517, places=3)
        self.assertAlmostEqual(rows[-1]["china_reserves"], 2346.446, places=3)
        self.assertAlmostEqual(
            rows[-1]["china_reserves"] - rows[-2]["china_reserves"], 14.930, places=3)

    def test_rejects_duplicate_gap_out_of_order_and_non_positive_values(self):
        cases = {
            "重复": ["2026-01-15,7419,,SAFE", "2026-01-31,7422,,SAFE"],
            "连续": ["2025-12-31,7415,,SAFE", "2026-02-28,7422,,SAFE"],
            "升序": ["2026-02-28,7422,,SAFE", "2026-01-31,7419,,SAFE"],
            "正数": ["2026-01-31,0,,SAFE"],
        }
        for message, rows in cases.items():
            with self.subTest(message=message):
                path = self.write_csv(rows)
                with self.assertRaisesRegex(ValueError, message):
                    build_site.read_official_reserve_rows(path)

    def test_future_month_is_validated_but_excluded_from_dashboard_cutoff(self):
        path = self.write_csv([
            "2026-05-31,7496,,SAFE",
            "2026-06-30,7544,,SAFE",
            "2026-07-31,7550,,SAFE",
        ])
        rows = build_site.read_official_reserve_rows(path, max_date=_date(2026, 7, 7))
        self.assertEqual([row["date"] for row in rows], ["2026-05-31", "2026-06-30"])

    def test_field_level_merge_preserves_global_reserve(self):
        base = [{
            "date": "2026-06-30", "_date": _date(2026, 6, 30),
            "china_reserves": 2321.5452, "global_reserves": 36558.5405,
        }]
        override = [{
            "date": "2026-06-30", "_date": _date(2026, 6, 30),
            "china_reserves_10k_oz": 7544.0, "china_reserves": 2346.446,
            "global_reserves": None, "china_source": "SAFE", "global_source": None,
        }]
        row = build_site.merge_reserve_rows(base, override)[0]
        self.assertAlmostEqual(row["china_reserves"], 2346.446)
        self.assertAlmostEqual(row["global_reserves"], 36558.5405)
        self.assertEqual(row["china_source"], "SAFE")
        self.assertIn("Excel", row["global_source"])
```

- [ ] **步骤 2：运行定向测试，确认先失败**

运行：

```bash
python3 -m unittest tests.test_build_site.OfficialReserveOverrideTest -v
```

预期：失败，错误包含 `AttributeError: module 'scripts.build_site' has no attribute 'read_official_reserve_rows'`。

- [ ] **步骤 3：把本地覆盖文件改成官方原始序列**

将 `data/market/official_reserves_manual.csv` 完整替换为：

```csv
date,china_reserves_10k_oz,global_reserves,source
2025-12-31,7415,,SAFE: 官方储备资产（2025）
2026-01-31,7419,,SAFE: 官方储备资产（2026）
2026-02-28,7422,,SAFE: 官方储备资产（2026）
2026-03-31,7438,,SAFE: 官方储备资产（2026）
2026-04-30,7464,,SAFE: 官方储备资产（2026）
2026-05-31,7496,,SAFE: 官方储备资产（2026）
2026-06-30,7544,,SAFE: 官方储备资产（2026）
```

- [ ] **步骤 4：实现读取、连续性校验和字段级合并**

在 `scripts/build_site.py` 的常量区和 CSV 读取函数之后加入：

```python
TEN_THOUSAND_TROY_OZ_TO_TONNES = 0.311034768


def month_index(value):
    return value.year * 12 + value.month


def optional_csv_float(value):
    return None if value in (None, "") else float(value)


def read_official_reserve_rows(path, max_date=None):
    if not path.exists():
        return []

    rows = []
    seen_months = set()
    previous_date = None
    previous_month = None
    with path.open(encoding="utf-8", newline="") as file:
        for raw in csv.DictReader(file):
            row_date = datetime.strptime(raw["date"], "%Y-%m-%d").date()
            current_month = month_index(row_date)
            if current_month in seen_months:
                raise ValueError(f"官方储备月份重复：{raw['date']}")
            if previous_date is not None and row_date <= previous_date:
                raise ValueError("官方储备日期必须严格升序")
            if previous_month is not None and current_month != previous_month + 1:
                raise ValueError("官方储备月份必须连续")

            ounces = float(raw["china_reserves_10k_oz"])
            if ounces <= 0:
                raise ValueError("中国官方黄金储备必须为正数")
            source = (raw.get("source") or "").strip()
            global_reserves = optional_csv_float(raw.get("global_reserves"))
            rows.append({
                "date": format_date(row_date),
                "_date": row_date,
                "china_reserves_10k_oz": ounces,
                "china_reserves": ounces * TEN_THOUSAND_TROY_OZ_TO_TONNES,
                "global_reserves": global_reserves,
                "china_source": source,
                "global_source": source if global_reserves is not None else None,
            })
            seen_months.add(current_month)
            previous_date = row_date
            previous_month = current_month

    return [row for row in rows if max_date is None or row["_date"] <= max_date]


def merge_reserve_rows(base_rows, override_rows):
    merged = {}
    for row in base_rows:
        item = {
            **row,
            "china_source": row.get("china_source") or "Excel: 官方黄金储备",
            "global_source": row.get("global_source") or "Excel: 官方黄金储备",
        }
        merged[(row["_date"].year, row["_date"].month)] = item

    for row in override_rows:
        key = (row["_date"].year, row["_date"].month)
        item = merged.get(key, {
            "date": row["date"], "_date": row["_date"],
            "china_reserves": None, "global_reserves": None,
            "china_source": None, "global_source": None,
        })
        item["china_reserves_10k_oz"] = row["china_reserves_10k_oz"]
        item["china_reserves"] = row["china_reserves"]
        item["china_source"] = row["china_source"]
        if row["global_reserves"] is not None:
            item["global_reserves"] = row["global_reserves"]
            item["global_source"] = row["global_source"]
        merged[key] = item

    return sorted(merged.values(), key=lambda row: row["_date"])
```

- [ ] **步骤 5：接入新读取器并把央行状态改成只看中国当月净增减**

在 `read_dashboard_data()` 中将旧的通用 CSV 读取和整行覆盖替换为：

```python
manual_reserve_rows = read_official_reserve_rows(
    OFFICIAL_RESERVES_MANUAL_FILE, max_date=today)
reserve_rows = merge_reserve_rows(reserve_rows, manual_reserve_rows)
```

在 `make_reserve_layer()` 中把状态逻辑替换为：

```python
state = state_from_delta(china_change, supportive_when="up", threshold=0.0)
```

并在返回的 `latest` 字典中加入：

```python
"china_source": latest.get("china_source") or "Excel: 官方黄金储备",
"global_source": (
    latest.get("global_source")
    if latest.get("global_reserves") is not None else None),
```

层级 `source` 使用上述 `china_source`；全球文字使用 `global_source`，不得因全球值缺失改变中国状态。

- [ ] **步骤 6：运行定向测试并更新现有官方储备集成断言**

把 `test_official_reserves_use_manual_china_update_without_future_excel_rows` 的来源和全球断言改为：

```python
self.assertAlmostEqual(layer["latest"]["china_change"], 14.93, places=2)
self.assertAlmostEqual(layer["latest"]["global_reserves"], 36558.5405)
self.assertIn("SAFE", layer["source"])
self.assertEqual(layer["state"], "supportive")
```

运行：

```bash
python3 -m unittest tests.test_build_site.OfficialReserveOverrideTest tests.test_build_site.GoldDashboardDataTest -v
```

预期：全部通过；页面集成断言不再出现“Wind 通讯社”或“全球本期未更新”。

- [ ] **步骤 7：提交任务 1**

```bash
git add data/market/official_reserves_manual.csv scripts/build_site.py tests/test_build_site.py
git commit -m "fix: reconcile China official gold reserves" -m "Co-Authored-By: OpenAI <codex@openai.com>"
```

---

### 任务 2：增加严格均线和短中期技术状态

**文件：**

- 修改：`scripts/build_site.py:250-263,803-843,1269-1331`
- 测试：`tests/test_build_site.py:181-223`

**接口：**

- 产出：`attach_gold_technicals(rows: list[dict]) -> list[dict]`
- 产出：`classify_technical_state(price, ma20, ma60, ma200, return_5d) -> dict[str, str]`
- 更新：`make_price_trend_layer()` 的 `latest` 增加 `ma20`、`ma60`、`ma200`、三条均线距离、`return_5d`、`momentum_3m`、`drawdown`；`technical` 增加短期、中期、排列和判断改变条件。
- 后续依赖：首屏价格区和交互图只读取 `price_trend` 层的技术字段和 `chart_rows`。

- [ ] **步骤 1：先写严格排列和短中期状态测试**

在 `PriceTrendTest` 中加入：

```python
    def test_classifies_strict_bullish_and_bearish_alignment(self):
        bullish = build_site.classify_technical_state(120, 115, 110, 100, 0.03)
        bearish = build_site.classify_technical_state(80, 85, 90, 100, -0.03)
        self.assertEqual(bullish, {
            "short_term": "短期反弹", "medium_term": "中期偏多", "alignment": "多头排列"})
        self.assertEqual(bearish, {
            "short_term": "短期走弱", "medium_term": "中期偏空", "alignment": "空头排列"})

    def test_does_not_call_mixed_mas_a_complete_alignment(self):
        state = build_site.classify_technical_state(107, 106, 110, 105, 0.02)
        self.assertEqual(state["short_term"], "短期反弹")
        self.assertEqual(state["medium_term"], "中期修复")
        self.assertEqual(state["alignment"], "未形成完整排列")

    def test_reports_insufficient_history_without_inventing_a_trend(self):
        state = build_site.classify_technical_state(101, None, None, None, None)
        self.assertEqual(state, {
            "short_term": "样本不足", "medium_term": "样本不足", "alignment": "样本不足"})

    def test_layer_exposes_all_mas_and_current_contract(self):
        layer = build_site.make_price_trend_layer(self._rising())
        self.assertTrue({"ma20", "ma60", "ma200", "return_5d"} <= set(layer["latest"]))
        self.assertEqual(layer["technical"]["alignment"], "多头排列")
        self.assertEqual(layer["technical"]["medium_term"], "中期偏多")
        self.assertTrue({"gap_ma20", "gap_ma60", "gap_ma200"} <= set(layer["latest"]))
        self.assertIn("跌破", layer["technical"]["trigger"])
```

- [ ] **步骤 2：运行定向测试，确认先失败**

```bash
python3 -m unittest tests.test_build_site.PriceTrendTest -v
```

预期：失败，错误指出 `classify_technical_state` 不存在或 `latest` 缺少均线字段。

- [ ] **步骤 3：实现均线附加和精确分类函数**

```python
def attach_gold_technicals(rows):
    output = attach_rolling_ma(rows, "gold_price", 20, "ma20")
    output = attach_rolling_ma(output, "gold_price", 60, "ma60")
    return attach_rolling_ma(output, "gold_price", 200, "ma200")


def classify_technical_state(price, ma20, ma60, ma200, return_5d):
    if ma20 is None or return_5d is None:
        short_term = "样本不足"
    elif price > ma20 and return_5d > 0:
        short_term = "短期反弹"
    elif price < ma20 and return_5d < 0:
        short_term = "短期走弱"
    else:
        short_term = "短期震荡"

    if ma60 is None or ma200 is None:
        medium_term = "样本不足"
    elif price > ma60 and price > ma200:
        medium_term = "中期偏多"
    elif price < ma60 and price < ma200:
        medium_term = "中期偏空"
    else:
        medium_term = "中期修复"

    if ma20 is None or ma60 is None or ma200 is None:
        alignment = "样本不足"
    elif price >= ma20 > ma60 > ma200:
        alignment = "多头排列"
    elif price <= ma20 < ma60 < ma200:
        alignment = "空头排列"
    else:
        alignment = "未形成完整排列"

    return {"short_term": short_term, "medium_term": medium_term, "alignment": alignment}
```

- [ ] **步骤 4：用新技术序列重写 `make_price_trend_layer()`**

保留原有关系检验兼容字段，但用以下完整计算合同构造层：

```python
def make_price_trend_layer(gold_rows):
    chart_rows = attach_gold_technicals(gold_rows)
    latest = latest_with_value(chart_rows, "gold_price")
    return_5d = trailing_return(chart_rows, "gold_price", 5)
    momentum_3m = trailing_return(chart_rows, "gold_price", 63)
    prices = [row["gold_price"] for row in chart_rows if row.get("gold_price") is not None]
    peak = max(prices[-252:]) if prices else None
    drawdown = pct_return(latest["gold_price"], peak) if peak else None
    technical = classify_technical_state(
        latest["gold_price"], latest.get("ma20"), latest.get("ma60"),
        latest.get("ma200"), return_5d)
    if technical["medium_term"] == "中期偏空":
        trigger = (
            f"重新站上 MA60 {fmt_number(latest.get('ma60'))} 与 "
            f"MA200 {fmt_number(latest.get('ma200'))} 后，转入中期修复。")
    elif technical["medium_term"] == "中期偏多":
        trigger = (
            f"同时跌破 MA60 {fmt_number(latest.get('ma60'))} 与 "
            f"MA200 {fmt_number(latest.get('ma200'))} 后，中期趋势转弱。")
    else:
        trigger = "等待价格同时站上或跌破 MA60 与 MA200，确认中期方向。"
    technical["trigger"] = trigger
    state = {
        "中期偏多": "supportive", "中期偏空": "headwind",
    }.get(technical["medium_term"], "neutral")

    return {
        "id": "price_trend",
        "name": "价格与趋势",
        "source": "Wind: SPTAUUSDOZ.IDC（派生 MA20/MA60/MA200、动量）",
        "frequency": "daily",
        "data_quality": "fresh",
        "state": state,
        "latest": {
            "date": latest["date"], "value": latest["gold_price"],
            "gold_price": latest["gold_price"], "ma20": latest.get("ma20"),
            "ma60": latest.get("ma60"), "ma200": latest.get("ma200"),
            "gap_ma20": pct_return(latest["gold_price"], latest.get("ma20")),
            "gap_ma60": pct_return(latest["gold_price"], latest.get("ma60")),
            "gap_ma200": pct_return(latest["gold_price"], latest.get("ma200")),
            "return_5d": return_5d, "momentum_3m": momentum_3m,
            "drawdown": drawdown,
        },
        "technical": technical,
        "change": momentum_3m,
        "change_since": None,
        "value_label": fmt_number(latest["gold_price"]),
        "change_label": fmt_pct(momentum_3m),
        "read": (
            f"{technical['medium_term']}，{technical['short_term']}，"
            f"{technical['alignment']}。"),
        "wrong_if": "价格重新站上或跌破中长期均线后，技术状态将按同一规则更新。",
        "next_trigger": "跟踪价格与 MA20、MA60、MA200 的相对位置。",
        "chart_key": "gold_price",
        "chart_rows": chart_rows,
    }
```

- [ ] **步骤 5：运行价格层和关系检验回归测试**

```bash
python3 -m unittest tests.test_build_site.PriceTrendTest -v
python3 -m unittest tests.test_build_site.GoldDashboardDataTest.test_relationships_explain_factor_usefulness_by_phase -v
```

预期：全部通过；关系检验仍使用未来收益，不受首屏分类变化影响。

- [ ] **步骤 6：提交任务 2**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: add explicit gold technical states" -m "Co-Authored-By: OpenAI <codex@openai.com>"
```

---

### 任务 3：把首页姿态压缩为五组驱动并生成三条变化

**文件：**

- 修改：`scripts/build_site.py:22-48,499-513,633-802,1269-1331,1883-1892`
- 测试：`tests/test_build_site.py:224-310`

**接口：**

- 产出：`combine_subsignal_states(states: list[str]) -> str`
- 产出：`score_driver_layers(layers: list[dict]) -> tuple[int, str, str, float, int]`
- 产出：`make_driver_rows(layers: list[dict]) -> list[dict]`，固定六行展示、五组计分。
- 产出：`make_recent_changes(layers: list[dict]) -> list[dict]`，固定返回 `macro`、`official`、`flows`。
- 更新：`read_dashboard_data()` 返回 `driver_layers`、`driver_rows`、`recent_changes` 和 `technical_layer`。

- [ ] **步骤 1：先写五组计分、资金合成和三条变化测试**

```python
class DriverPostureTest(unittest.TestCase):
    def layer(self, layer_id, state, quality="fresh"):
        return {"id": layer_id, "state": state, "data_quality": quality}

    def test_scores_only_five_driver_groups(self):
        layers = [
            self.layer("real_rate", "headwind"),
            self.layer("dollar", "headwind"),
            self.layer("inflation_expectation", "headwind"),
            self.layer("official_reserves", "supportive"),
            self.layer("positioning_technical", "neutral"),
            self.layer("price_trend", "supportive"),
            self.layer("epu", "supportive"),
            self.layer("gpr", "supportive"),
        ]
        score, posture, _, tendency, active = build_site.score_driver_layers(layers)
        self.assertEqual((score, posture, active), (-2, "承压", 5))
        self.assertAlmostEqual(tendency, -0.4)

    def test_excludes_missing_and_very_stale_and_requires_three_groups(self):
        layers = [
            self.layer("real_rate", "headwind"),
            self.layer("dollar", "headwind", "very-stale"),
            self.layer("inflation_expectation", "missing", "missing"),
            self.layer("official_reserves", "supportive"),
        ]
        self.assertEqual(build_site.score_driver_layers(layers)[1], "数据不足")
        self.assertEqual(build_site.score_driver_layers(layers)[4], 2)

    def test_combines_etf_and_cot_without_gold_price_vote(self):
        self.assertEqual(
            build_site.combine_subsignal_states(["headwind", "supportive"]), "neutral")
        self.assertEqual(
            build_site.combine_subsignal_states(["supportive", "neutral"]), "supportive")
        self.assertEqual(
            build_site.combine_subsignal_states(["missing", "headwind"]), "headwind")
        self.assertEqual(
            build_site.combine_subsignal_states(["missing", "missing"]), "missing")

    def test_dashboard_exposes_six_rows_but_five_scored_groups(self):
        dashboard = build_site.read_dashboard_data(today=_date(2026, 7, 7))
        self.assertEqual(len(dashboard["driver_layers"]), 5)
        self.assertEqual(
            [row["id"] for row in dashboard["driver_rows"]],
            ["real_rate", "dollar", "official_reserves", "inflation_expectation", "etf", "cot"])
        self.assertEqual(
            [item["id"] for item in dashboard["recent_changes"]],
            ["macro", "official", "flows"])
```

- [ ] **步骤 2：运行定向测试，确认先失败**

```bash
python3 -m unittest tests.test_build_site.DriverPostureTest -v
```

预期：失败，缺少 `score_driver_layers`、`combine_subsignal_states` 或 dashboard 新字段。

- [ ] **步骤 3：实现五组选择和资金子信号合成**

```python
DRIVER_LAYER_IDS = (
    "real_rate", "dollar", "inflation_expectation",
    "official_reserves", "positioning_technical",
)


def combine_subsignal_states(states):
    weights = {"supportive": 1, "neutral": 0, "headwind": -1}
    votes = [weights[state] for state in states if state in weights]
    if not votes:
        return "missing"
    average = sum(votes) / len(votes)
    if average >= 0.5:
        return "supportive"
    if average <= -0.5:
        return "headwind"
    return "neutral"


def score_driver_layers(layers):
    selected = [
        layer for layer in layers
        if layer["id"] in DRIVER_LAYER_IDS
        and layer.get("state") in {"supportive", "neutral", "headwind"}
        and layer.get("data_quality") != "very-stale"
    ]
    if len(selected) < 3:
        return 0, "数据不足", "missing", 0.0, len(selected)
    return score_layers(selected)
```

在 `make_positioning_layer()` 中：

1. 将 ETF 缺值时的状态设为 `missing`；
2. 将 CFTC 缺值时的状态设为 `missing`；
3. 删除 `gold_state` 对 `state_score` 的投票；
4. 使用以下合成并暴露子状态：

```python
state = combine_subsignal_states([etf_state, cot_state])
sub_states = {"etf": etf_state, "cot": cot_state}
```

在返回字典中增加 `"sub_states": sub_states`；GVZ 和金价仍可保留为背景字段，但不参与 `state`。

- [ ] **步骤 4：实现六行展示结构和三条主题变化**

新增通用行辅助函数和固定行列表：

```python
def driver_row(row_id, name, category, state, value, change, read, quality, date_value, source):
    return {
        "id": row_id, "name": name, "category": category, "state": state,
        "value": value, "change": change, "read": read, "quality": quality,
        "date": date_value, "source": source,
    }


def make_driver_rows(layers):
    by_id = {layer["id"]: layer for layer in layers}
    real = by_id["real_rate"]
    dollar = by_id["dollar"]
    inflation = by_id["inflation_expectation"]
    reserve = by_id["official_reserves"]
    positioning = by_id["positioning_technical"]
    latest_positioning = positioning["latest"]
    common = lambda layer: (
        layer["data_quality"], layer["latest"].get("date"), layer["source"])

    rows = [
        driver_row("real_rate", "实际利率", "宏观", real["state"], real["value_label"],
                   real["change_label"], "机会成本变化", *common(real)),
        driver_row("dollar", "美元", "宏观", dollar["state"], dollar["value_label"],
                   dollar["change_label"], "美元计价压力", *common(dollar)),
        driver_row("official_reserves", "中国央行购金", "官方部门", reserve["state"],
                   fmt_signed(reserve["latest"].get("china_change"), suffix=" 吨"),
                   "较上月增量", "官方购金慢变量", *common(reserve)),
        driver_row("inflation_expectation", "通胀预期", "宏观", inflation["state"],
                   inflation["value_label"], inflation["change_label"],
                   "通胀补偿变化", *common(inflation)),
        driver_row("etf", "黄金 ETF", "资金流", positioning["sub_states"]["etf"],
                   f"{fmt_number(latest_positioning.get('etf_total'))} 吨",
                   fmt_signed(latest_positioning.get("etf_change"), suffix=" 吨"),
                   "黄金 ETF 合计持仓", *common(positioning)),
        driver_row("cot", "CFTC 净多", "仓位", positioning["sub_states"]["cot"],
                   f"{fmt_integer(latest_positioning.get('managed_money_net'))} 张",
                   fmt_signed_integer(latest_positioning.get("managed_money_net_change"), suffix=" 张"),
                   "Managed Money 近 4 周变化", *common(positioning)),
    ]
    return rows
```

新增三条变化函数，返回结构固定：

```python
def make_recent_changes(layers):
    by_id = {layer["id"]: layer for layer in layers}
    real = by_id["real_rate"]
    dollar = by_id["dollar"]
    reserve = by_id["official_reserves"]
    positioning = by_id["positioning_technical"]

    if real["state"] == dollar["state"] == "headwind":
        macro_tone, macro_headline = "headwind", "实际利率与美元同向上行"
    elif real["state"] == dollar["state"] == "supportive":
        macro_tone, macro_headline = "supportive", "实际利率与美元同向回落"
    else:
        macro_tone, macro_headline = "neutral", "实际利率与美元信号分化"

    chart_rows = reserve.get("chart_rows", [])
    current_purchase = reserve["latest"].get("china_change")
    previous_purchase = chart_rows[-2].get("china_mom_change") if len(chart_rows) >= 2 else None
    if current_purchase is None:
        official_tone, official_headline = "missing", "中国央行购金数据缺失"
    elif current_purchase < 0:
        official_tone, official_headline = "headwind", "中国央行转为净卖出"
    elif previous_purchase is not None and current_purchase > previous_purchase:
        official_tone, official_headline = "supportive", "中国央行购金加速"
    elif previous_purchase is not None and current_purchase < previous_purchase:
        official_tone, official_headline = "supportive", "中国央行仍在净买入，但速度放缓"
    else:
        official_tone, official_headline = "supportive", "中国央行继续净买入"

    etf_state = positioning["sub_states"]["etf"]
    cot_state = positioning["sub_states"]["cot"]
    if etf_state == cot_state and etf_state in {"supportive", "headwind"}:
        flows_tone = etf_state
        flows_headline = "ETF 与 CFTC 仓位同向"
    else:
        flows_tone, flows_headline = "neutral", "ETF 与 CFTC 仓位分化"

    return [
        {"id": "macro", "tone": macro_tone, "label": "宏观条件",
         "headline": macro_headline,
         "detail": f"实际利率 {real['change_label']}；美元 {dollar['change_label']}。"},
        {"id": "official", "tone": official_tone, "label": "官方购金",
         "headline": official_headline,
         "detail": (
             f"本月 {fmt_signed(current_purchase, suffix=' 吨')}；"
             f"上月 {fmt_signed(previous_purchase, suffix=' 吨')}。")},
        {"id": "flows", "tone": flows_tone, "label": "资金与仓位",
         "headline": flows_headline,
         "detail": (
             f"ETF {fmt_signed(positioning['latest'].get('etf_change'), suffix=' 吨')}；"
             f"CFTC {fmt_signed_integer(positioning['latest'].get('managed_money_net_change'), suffix=' 张')}。")},
    ]
```

- [ ] **步骤 5：在 `read_dashboard_data()` 中按质量计算五组姿态并返回新字段**

在现有 staleness 循环之后加入：

```python
driver_layers = [layer for layer in layers if layer["id"] in DRIVER_LAYER_IDS]
score, posture, posture_state, tendency, active_layers = score_driver_layers(driver_layers)
technical_layer = next(layer for layer in layers if layer["id"] == "price_trend")
driver_rows = make_driver_rows(layers)
recent_changes = make_recent_changes(layers)
```

在 dashboard 返回字典中增加上述四个字段，并用新五组结果覆盖旧的姿态字段。

- [ ] **步骤 6：运行驱动、姿态和全数据层定向测试**

```bash
python3 -m unittest tests.test_build_site.DriverPostureTest tests.test_build_site.PostureTest tests.test_build_site.GoldDashboardDataTest.test_reads_refreshed_workbook_into_driver_layers -v
```

预期：全部通过；低层 `score_layers()` 边界测试继续保留，首页改用 `score_driver_layers()`。

- [ ] **步骤 7：提交任务 3**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: focus gold posture on five drivers" -m "Co-Authored-By: OpenAI <codex@openai.com>"
```

---

### 任务 4：重构为决策优先的服务器渲染页面

**文件：**

- 修改：`scripts/build_site.py:1848-2403`
- 测试：`tests/test_build_site.py:89-180`

**接口：**

- 产出：`make_price_summary_section(technical_layer: dict) -> str`
- 产出：`make_driver_section(rows: list[dict]) -> str`
- 产出：`make_recent_changes_section(changes: list[dict]) -> str`
- 产出：`make_research_details(dashboard: dict) -> str`
- 更新：`build_html()` 只组装顶栏、价格与技术、主要驱动、三条变化、折叠研究五个区域。
- 后续依赖：任务 5 只替换 `make_price_summary_section()` 内的静态价格图，不重改页面骨架。

- [ ] **步骤 1：先把 HTML 合同测试改成新信息层级**

在 `GoldDashboardDataTest` 中新增：

```python
    def test_html_uses_decision_first_structure(self):
        html = build_site.build_html(build_site.read_dashboard_data(today=self.TODAY))
        self.assertIn('id="gold-price-section"', html)
        self.assertIn('id="driver-table"', html)
        self.assertIn('id="recent-changes"', html)
        self.assertEqual(html.count('class="recent-change '), 3)
        self.assertIn('<details class="research-evidence">', html)
        self.assertIn("研究依据", html)
        self.assertNotIn("<h2>What Changed</h2>", html)
        self.assertNotIn("<h2>失效条件</h2>", html)
        self.assertNotIn("<h2>下一观察点</h2>", html)

    def test_price_and_proxy_layers_remain_in_research_but_not_headline_score(self):
        dashboard = build_site.read_dashboard_data(today=self.TODAY)
        self.assertEqual(
            [layer["id"] for layer in dashboard["driver_layers"]],
            ["real_rate", "dollar", "inflation_expectation", "official_reserves", "positioning_technical"])
        html = build_site.build_html(dashboard)
        self.assertIn("经济政策不确定性", html)
        self.assertIn("地缘政治风险", html)
        self.assertIn("因子关系检验", html)
        self.assertIn("中国：SAFE", html)
        self.assertIn("全球：Excel: 官方黄金储备", html)
```

更新旧的 `test_html_is_data_driven_and_excludes_opinion_summary_text`：删除对旧三列标题和英文
`tendency` 的正向断言，改为断言中文姿态、数据质量和来源仍存在。

- [ ] **步骤 2：运行新 HTML 合同测试，确认先失败**

```bash
python3 -m unittest tests.test_build_site.GoldDashboardDataTest.test_html_uses_decision_first_structure -v
```

预期：失败，找不到 `gold-price-section` 或折叠研究区。

- [ ] **步骤 3：实现四个小渲染函数**

价格区先用现有静态 sparkline，任务 5 再增强：

```python
def make_price_summary_section(technical_layer):
    latest = technical_layer["latest"]
    technical = technical_layer["technical"]
    chart = make_sparkline(
        technical_layer["chart_rows"], "gold_price", color="#b88728",
        width=720, height=240, limit=252)
    return f"""
    <section id="gold-price-section" class="price-stage">
      <div class="price-panel">
        <div class="section-eyebrow">黄金现货 · 美元/盎司</div>
        <div class="price-value">{fmt_number(latest['gold_price'])}</div>
        <div class="price-move">近 5 个有效观测日 {fmt_pct(latest.get('return_5d'))}</div>
        {chart}
      </div>
      <aside class="technical-panel state-{technical_layer['state']}">
        <div class="section-eyebrow">技术状态</div>
        <h2>{escape(technical['medium_term'])} · {escape(technical['short_term'])}</h2>
        <p>{escape(technical['alignment'])}</p>
        <dl class="ma-summary">
          <div><dt>MA20</dt><dd>{fmt_number(latest.get('ma20'))} · {fmt_pct(latest.get('gap_ma20'))}</dd></div>
          <div><dt>MA60</dt><dd>{fmt_number(latest.get('ma60'))} · {fmt_pct(latest.get('gap_ma60'))}</dd></div>
          <div><dt>MA200</dt><dd>{fmt_number(latest.get('ma200'))} · {fmt_pct(latest.get('gap_ma200'))}</dd></div>
        </dl>
        <p class="technical-trigger"><strong>判断改变条件：</strong>{escape(technical['trigger'])}</p>
      </aside>
    </section>
    """


def make_driver_section(rows):
    body = "".join(f"""
      <div class="driver-row" data-driver-id="{escape(row['id'])}">
        <span class="driver-state state-{escape(row['state'])}">{state_badge(row['state'])}</span>
        <div><strong>{escape(row['name'])}</strong><small>{escape(row['category'])}</small></div>
        <strong>{escape(row['value'])}</strong>
        <span>{escape(row['change'])}</span>
        <span>{escape(row['read'])}</span>
        <span class="quality">{escape(quality_badge(row['quality']))}</span>
      </div>
    """ for row in rows)
    return f"""
    <section class="driver-panel">
      <div class="section-heading"><h2>当前主要驱动</h2><span>按决策相关性排序</span></div>
      <div id="driver-table" class="driver-table">{body}</div>
    </section>
    """


def make_recent_changes_section(changes):
    cards = "".join(f"""
      <article class="recent-change tone-{escape(item['tone'])}">
        <small>{escape(item['label'])}</small>
        <h3>{escape(item['headline'])}</h3>
        <p>{escape(item['detail'])}</p>
      </article>
    """ for item in changes)
    return f"<aside id=\"recent-changes\" class=\"recent-panel\"><h2>最近发生了什么</h2>{cards}</aside>"
```

将现有关系区、央行图、数据质量表和估值文字放入：

```python
def make_research_details(dashboard):
    relationship_section = make_relationship_section(dashboard["relationships"])
    china_chart = make_bar_chart(
        dashboard["reserve_rows"], "china_mom_change",
        "中国央行黄金储备环比变化柱状图", "china-bar")
    global_chart = make_bar_chart(
        dashboard["reserve_rows"], "global_mom_change",
        "全球央行黄金储备环比变化柱状图", "global-bar")
    reserve_layer = next(layer for layer in dashboard["layers"] if layer["id"] == "official_reserves")
    reserve_sources = (
        f"中国：{reserve_layer['latest'].get('china_source') or '—'}；"
        f"全球：{reserve_layer['latest'].get('global_source') or '—'}")
    quality_rows = "".join(f"""
      <tr><td>{escape(layer['name'])}</td><td>{escape(quality_badge(layer['data_quality']))}</td>
      <td>{'—' if layer.get('lag_days') is None else str(layer['lag_days']) + ' 天'}</td>
      <td>{escape(layer['latest'].get('date') or '—')}</td><td>{escape(layer['source'])}</td></tr>
    """ for layer in dashboard["layers"])
    return f"""
    <details class="research-evidence">
      <summary><strong>研究依据</strong><span>关系检验 · 历史阶段 · 数据来源与方法</span></summary>
      {relationship_section}
      <section class="wide"><h2>央行购金历史</h2><div class="chart-pair">
        <div class="chart-box">{china_chart}</div><div class="chart-box">{global_chart}</div>
      </div><p class="method">{escape(reserve_sources)}</p></section>
      <section class="wide"><h2>数据质量与来源</h2><table><tbody>{quality_rows}</tbody></table></section>
    </details>
    """
```

- [ ] **步骤 4：替换 `build_html()` 的首屏正文和布局 CSS**

在保留现有关系图、表格和 SVG 通用样式的前提下，先计算顶栏文字：

```python
scored_states = [
    layer["state"] for layer in dashboard["driver_layers"]
    if layer["state"] in {"supportive", "neutral", "headwind"}
    and layer.get("data_quality") != "very-stale"
]
summary = (
    f"{scored_states.count('headwind')} 项压力 · "
    f"{scored_states.count('supportive')} 项支持 · "
    f"{scored_states.count('neutral')} 项中性")
updated = f"主要市场数据截至 {dashboard['technical_layer']['latest']['date']}"
```

正文固定为：

```html
<main>
  <header class="decision-header">
    <div><div class="kicker">Gold decision dashboard</div><h1>{title}</h1><p>{updated}</p></div>
    <aside class="posture state-{posture_state}"><span>驱动合成</span><strong>{posture}</strong><small>{summary}</small></aside>
  </header>
  {price_section}
  <section class="decision-grid">
    {driver_section}
    {recent_changes_section}
  </section>
  {research_details}
</main>
```

其中 `{title}`、`{updated}`、`{posture_state}`、`{posture}`、`{summary}` 和四个 section
都由 Python 先生成并经过 `escape()`。新增布局 CSS 使用以下确定结构：

```css
.decision-header{display:grid;grid-template-columns:minmax(0,1fr) 220px;gap:20px;align-items:end}
.price-stage{display:grid;grid-template-columns:minmax(0,1.8fr) minmax(270px,.8fr);gap:16px;margin-top:18px}
.price-panel,.technical-panel,.driver-panel,.recent-panel,.research-evidence{border:1px solid var(--line);border-radius:14px;background:var(--panel)}
.price-panel,.technical-panel,.driver-panel,.recent-panel{padding:18px}
.price-value{font-size:36px;font-weight:800;letter-spacing:-.03em}
.ma-summary{display:grid;gap:8px;margin:16px 0 0}
.ma-summary div{display:flex;justify-content:space-between;border-top:1px solid var(--line);padding-top:8px}
.decision-grid{display:grid;grid-template-columns:minmax(0,1.5fr) minmax(290px,.8fr);gap:16px;margin-top:16px}
.driver-row{display:grid;grid-template-columns:62px minmax(110px,1fr) 100px 110px minmax(150px,1.2fr) 62px;gap:10px;align-items:center;padding:10px 0;border-top:1px solid var(--line);font-size:12px}
.driver-row:first-child{border-top:0}.driver-row small{display:block;color:var(--muted)}
.recent-change{padding:12px;border-left:3px solid var(--neutral);background:#f7f8f5;border-radius:9px}.recent-change+.recent-change{margin-top:10px}
.tone-supportive{border-left-color:var(--support)}.tone-headwind{border-left-color:var(--headwind)}
.research-evidence{margin-top:16px;padding:0 16px 16px}.research-evidence>summary{cursor:pointer;padding:16px 0;display:flex;justify-content:space-between}
@media(max-width:900px){.decision-header,.price-stage,.decision-grid{grid-template-columns:1fr}.driver-row{grid-template-columns:54px 1fr 90px 90px}.driver-row>span:nth-last-child(-n+2){grid-column:2/-1}}
@media(max-width:560px){main{padding:18px 12px 32px}.driver-row{grid-template-columns:48px 1fr}.driver-row>*{grid-column:auto}.driver-row>strong,.driver-row>span:nth-child(n+3){grid-column:2/-1}}
```

- [ ] **步骤 5：运行 HTML、关系检验和图表回归测试**

```bash
python3 -m unittest \
  tests.test_build_site.GoldDashboardDataTest.test_html_uses_decision_first_structure \
  tests.test_build_site.GoldDashboardDataTest.test_price_and_proxy_layers_remain_in_research_but_not_headline_score \
  tests.test_build_site.GoldDashboardDataTest.test_relationships_explain_factor_usefulness_by_phase \
  tests.test_build_site.GoldDashboardDataTest.test_charts_show_axes_and_underlying_series -v
```

预期：全部通过；关系和历史表仍在 HTML 中，但被 `<details>` 包裹且默认关闭。

- [ ] **步骤 6：提交任务 4**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: restructure gold dashboard summary" -m "Co-Authored-By: OpenAI <codex@openai.com>"
```

---

### 任务 5：增加无依赖的价格区间、均线开关和 tooltip

**文件：**

- 修改：`scripts/build_site.py:1-4,1332-1595,1893-2403`
- 测试：`tests/test_build_site.py:1-4,89-180`

**接口：**

- 产出：`shift_months(value: date, months: int) -> date`
- 产出：`build_gold_chart_ranges(rows: list[dict]) -> dict[str, list[dict]]`
- 产出：`make_gold_chart_geometry(rows: list[dict], width=720, height=280) -> list[dict]`
- 产出：`make_gold_chart_svg(points: list[dict], range_id: str, hidden: bool) -> str`
- 产出：`make_gold_chart_panel(technical_layer: dict) -> str`
- 产出：常量 `GOLD_CHART_SCRIPT`，只处理 DOM 交互。

- [ ] **步骤 1：先写区间、payload 和 HTML 控件测试**

在测试文件增加 `json`、`re` 导入，并加入：

```python
import json
import re


class InteractiveGoldChartTest(unittest.TestCase):
    def test_calendar_ranges_end_at_latest_observation(self):
        dashboard = build_site.read_dashboard_data(today=_date(2026, 7, 7))
        rows = dashboard["technical_layer"]["chart_rows"]
        ranges = build_site.build_gold_chart_ranges(rows)
        self.assertEqual(set(ranges), {"3m", "1y", "3y"})
        self.assertEqual(ranges["1y"][-1]["date"], rows[-1]["date"])
        self.assertGreaterEqual(ranges["3m"][0]["_date"], _date(2026, 4, 6))

    def test_html_embeds_static_fallback_controls_and_safe_payload(self):
        html = build_site.build_html(build_site.read_dashboard_data(today=_date(2026, 7, 7)))
        self.assertIn('data-chart-range="3m"', html)
        self.assertIn('data-chart-range="1y" aria-pressed="true"', html)
        self.assertIn('data-chart-range="3y"', html)
        for series in ["ma20", "ma60", "ma200"]:
            self.assertIn(f'data-chart-series="{series}"', html)
        self.assertIn('data-range="1y"', html)
        self.assertIn('id="gold-chart-data"', html)
        self.assertIn("pointermove", html)

        raw = re.search(
            r'<script type="application/json" id="gold-chart-data">(.*?)</script>',
            html, re.S).group(1)
        payload = json.loads(raw)
        self.assertEqual(set(payload["ranges"]), {"3m", "1y", "3y"})
        latest = payload["ranges"]["1y"][-1]
        self.assertTrue({"date", "gold_price", "ma20", "ma60", "ma200", "x", "gold_price_y"} <= set(latest))

    def test_disables_ma_controls_without_enough_history(self):
        from datetime import timedelta
        start = _date(2026, 1, 1)
        rows = [{
            "date": (start + timedelta(days=i)).isoformat(),
            "_date": start + timedelta(days=i),
            "gold_price": 100.0 + i,
        } for i in range(30)]
        panel = build_site.make_gold_chart_panel(build_site.make_price_trend_layer(rows))
        self.assertIn('data-chart-series="ma20" aria-pressed="true"', panel)
        self.assertIn('data-chart-series="ma60" aria-pressed="false" disabled', panel)
        self.assertIn('data-chart-series="ma200" aria-pressed="false" disabled', panel)
```

- [ ] **步骤 2：运行图表测试，确认先失败**

```bash
python3 -m unittest tests.test_build_site.InteractiveGoldChartTest -v
```

预期：失败，缺少 `build_gold_chart_ranges`、交互按钮或 JSON payload。

- [ ] **步骤 3：实现日历区间和共享 SVG 几何**

在 `scripts/build_site.py` 增加 `calendar`、`json` 导入和以下函数：

```python
import calendar
import json


GOLD_CHART_RANGES = {"3m": 3, "1y": 12, "3y": 36}
GOLD_CHART_KEYS = ("gold_price", "ma20", "ma60", "ma200")


def shift_months(value, months):
    month_zero = value.year * 12 + value.month - 1 + months
    year, month_zero = divmod(month_zero, 12)
    month = month_zero + 1
    day = min(value.day, calendar.monthrange(year, month)[1])
    return date(year, month, day)


def build_gold_chart_ranges(rows):
    latest_date = rows[-1]["_date"]
    return {
        range_id: [row for row in rows if row["_date"] >= shift_months(latest_date, -months)]
        for range_id, months in GOLD_CHART_RANGES.items()
    }


def make_gold_chart_geometry(rows, width=720, height=280):
    left, right, top, bottom = 54, 18, 18, 34
    plot_width = width - left - right
    plot_height = height - top - bottom
    values = [
        row[key] for row in rows for key in GOLD_CHART_KEYS
        if row.get(key) is not None
    ]
    low, high = min(values), max(values)
    span = high - low or 1.0
    denominator = max(1, len(rows) - 1)
    points = []
    for index, row in enumerate(rows):
        point = {
            "date": row["date"],
            "x": round(left + index / denominator * plot_width, 2),
        }
        for key in GOLD_CHART_KEYS:
            value = row.get(key)
            point[key] = value
            point[f"{key}_y"] = (
                None if value is None
                else round(top + (high - value) / span * plot_height, 2)
            )
        points.append(point)
    return points


def svg_series_path(points, key):
    commands = []
    drawing = False
    for point in points:
        y = point.get(f"{key}_y")
        if y is None:
            drawing = False
            continue
        commands.append(f"{'L' if drawing else 'M'}{point['x']:.2f},{y:.2f}")
        drawing = True
    return " ".join(commands)
```

- [ ] **步骤 4：生成三个服务器 SVG 和安全 JSON**

```python
def make_gold_chart_svg(points, range_id, hidden=False, width=720, height=280):
    hidden_attr = " hidden" if hidden else ""
    paths = "".join(
        f'<path class="chart-series series-{key}" d="{svg_series_path(points, key)}"></path>'
        for key in GOLD_CHART_KEYS
    )
    first_date = points[0]["date"] if points else "—"
    last_date = points[-1]["date"] if points else "—"
    return f"""
    <svg class="gold-chart" data-range="{range_id}" viewBox="0 0 {width} {height}"
         role="img" aria-label="黄金价格与移动均线"{hidden_attr}>
      <line x1="54" y1="246" x2="702" y2="246" class="x-axis"></line>
      {paths}
      <line data-hover-line x1="54" y1="18" x2="54" y2="246" class="hover-line" hidden></line>
      <circle data-hover-price cx="54" cy="18" r="4" class="hover-price" hidden></circle>
      <text x="54" y="270" class="chart-label">{escape(first_date)}</text>
      <text x="702" y="270" text-anchor="end" class="chart-label">{escape(last_date)}</text>
    </svg>
    """


def safe_json(value):
    return json.dumps(value, ensure_ascii=False, separators=(",", ":")).replace("<", "\\u003c")


def make_gold_chart_panel(technical_layer):
    ranges = build_gold_chart_ranges(technical_layer["chart_rows"])
    geometries = {key: make_gold_chart_geometry(rows) for key, rows in ranges.items()}
    charts = "".join(
        make_gold_chart_svg(geometries[key], key, hidden=(key != "1y"))
        for key in ("3m", "1y", "3y")
    )
    payload = safe_json({"ranges": geometries})
    latest = technical_layer["latest"]
    def series_button(key, label):
        available = latest.get(key) is not None
        pressed = "true" if available else "false"
        disabled = "" if available else " disabled"
        return (
            f'<button type="button" data-chart-series="{key}" '
            f'aria-pressed="{pressed}"{disabled}>{label}</button>')
    return f"""
    <div class="gold-chart-panel" data-gold-chart>
      <div class="chart-controls" aria-label="黄金图表控制">
        <div class="range-controls">
          <button type="button" data-chart-range="3m" aria-pressed="false">3个月</button>
          <button type="button" data-chart-range="1y" aria-pressed="true">1年</button>
          <button type="button" data-chart-range="3y" aria-pressed="false">3年</button>
        </div>
        <div class="series-controls">
          {series_button("ma20", "MA20")}
          {series_button("ma60", "MA60")}
          {series_button("ma200", "MA200")}
        </div>
      </div>
      <div class="chart-stack">{charts}</div>
      <div class="chart-tooltip" aria-live="polite">
        <strong data-tooltip-date>—</strong>
        <span>金价 <b data-tooltip-price>—</b></span>
        <span>MA20 <b data-tooltip-ma20>—</b></span>
        <span>MA60 <b data-tooltip-ma60>—</b></span>
        <span>MA200 <b data-tooltip-ma200>—</b></span>
      </div>
      <script type="application/json" id="gold-chart-data">{payload}</script>
    </div>
    """
```

在 `make_price_summary_section()` 中用 `make_gold_chart_panel(technical_layer)` 替换静态 sparkline。

- [ ] **步骤 5：加入只操作 DOM 的完整交互脚本**

将以下脚本放在 `</body>` 前：

```javascript
(() => {
  const root = document.querySelector("[data-gold-chart]");
  if (!root) return;
  const payload = JSON.parse(document.getElementById("gold-chart-data").textContent);
  const rangeButtons = [...root.querySelectorAll("[data-chart-range]")];
  const seriesButtons = [...root.querySelectorAll("[data-chart-series]")];
  const charts = [...root.querySelectorAll(".gold-chart")];
  let activeRange = "1y";

  const setText = (selector, value) => {
    root.querySelector(selector).textContent = value == null ? "—" : Number(value).toLocaleString("zh-CN", {maximumFractionDigits: 2});
  };

  const showPoint = (svg, event) => {
    const points = payload.ranges[activeRange];
    if (!points.length) return;
    const rect = svg.getBoundingClientRect();
    const viewX = (event.clientX - rect.left) / rect.width * 720;
    const ratio = Math.max(0, Math.min(1, (viewX - 54) / 648));
    const point = points[Math.round(ratio * (points.length - 1))];
    const line = svg.querySelector("[data-hover-line]");
    const marker = svg.querySelector("[data-hover-price]");
    line.hidden = false;
    marker.hidden = false;
    line.setAttribute("x1", point.x);
    line.setAttribute("x2", point.x);
    marker.setAttribute("cx", point.x);
    marker.setAttribute("cy", point.gold_price_y);
    root.querySelector("[data-tooltip-date]").textContent = point.date;
    setText("[data-tooltip-price]", point.gold_price);
    setText("[data-tooltip-ma20]", point.ma20);
    setText("[data-tooltip-ma60]", point.ma60);
    setText("[data-tooltip-ma200]", point.ma200);
  };

  rangeButtons.forEach((button) => button.addEventListener("click", () => {
    activeRange = button.dataset.chartRange;
    rangeButtons.forEach((item) => item.setAttribute("aria-pressed", String(item === button)));
    charts.forEach((svg) => { svg.hidden = svg.dataset.range !== activeRange; });
    const activePoints = payload.ranges[activeRange];
    const point = activePoints[activePoints.length - 1];
    root.querySelector("[data-tooltip-date]").textContent = point.date;
    setText("[data-tooltip-price]", point.gold_price);
    setText("[data-tooltip-ma20]", point.ma20);
    setText("[data-tooltip-ma60]", point.ma60);
    setText("[data-tooltip-ma200]", point.ma200);
  }));

  seriesButtons.forEach((button) => button.addEventListener("click", () => {
    const next = button.getAttribute("aria-pressed") !== "true";
    button.setAttribute("aria-pressed", String(next));
    root.classList.toggle(`hide-${button.dataset.chartSeries}`, !next);
  }));

  charts.forEach((svg) => {
    svg.addEventListener("pointermove", (event) => showPoint(svg, event));
    svg.addEventListener("pointerdown", (event) => showPoint(svg, event));
  });
  rangeButtons.find((button) => button.dataset.chartRange === "1y").click();
})();
```

新增样式：

```css
.chart-controls{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap}.chart-controls button{border:1px solid var(--line);border-radius:999px;background:#f7f8f5;padding:5px 10px;cursor:pointer}.chart-controls button[aria-pressed="true"]{background:#2f5e4c;color:#fff;border-color:#2f5e4c}
.gold-chart{width:100%;height:auto;touch-action:pan-y}.gold-chart[hidden]{display:none}.chart-series{fill:none;stroke-width:2}.series-gold_price{stroke:var(--gold);stroke-width:3}.series-ma20{stroke:var(--support);stroke-dasharray:5 4}.series-ma60{stroke:var(--headwind);stroke-dasharray:3 4}.series-ma200{stroke:var(--blue);stroke-dasharray:7 4}
.hide-ma20 .series-ma20,.hide-ma60 .series-ma60,.hide-ma200 .series-ma200{display:none}.hover-line{stroke:var(--muted);stroke-dasharray:2 3}.hover-price{fill:var(--gold);stroke:#fff;stroke-width:2}.chart-tooltip{display:flex;gap:10px;flex-wrap:wrap;color:var(--muted);font-size:12px}.chart-tooltip strong{color:var(--text)}
```

- [ ] **步骤 6：运行交互 HTML 测试和完整 `test_build_site`**

```bash
python3 -m unittest tests.test_build_site.InteractiveGoldChartTest -v
python3 -m unittest tests.test_build_site -v
```

预期：全部通过；payload 可被 `json.loads()` 解析，默认一年静态 SVG 在 JavaScript 执行前已存在。

- [ ] **步骤 7：提交任务 5**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: add interactive gold price chart" -m "Co-Authored-By: OpenAI <codex@openai.com>"
```

---

### 任务 6：生成页面并完成独立验收

**文件：**

- 生成：`site/index.html`
- 复核：`scripts/build_site.py`
- 复核：`tests/test_build_site.py`
- 复核：`data/market/official_reserves_manual.csv`

**接口：** 无新接口；本任务只验证任务 1—5 的组合行为并提交生成物。

- [ ] **步骤 1：运行完整自动测试**

```bash
python3 -m unittest tests.test_build_site tests.test_refresh_wind_data tests.test_update_data_and_commit -v
```

预期：所有测试通过，输出结尾为 `OK`，失败和错误数均为 0。

- [ ] **步骤 2：重新生成静态页面并检查差异**

```bash
python3 scripts/build_site.py
git diff --check -- data/market/official_reserves_manual.csv scripts/build_site.py tests/test_build_site.py site/index.html
git status --short
```

预期：构建输出 `Wrote .../site/index.html`；`git diff --check` 退出码为 0；状态中只出现本计划文件、预期实现文件、生成页面以及既有未跟踪研究笔记。

- [ ] **步骤 3：用结构化检查确认静态降级合同**

```bash
python3 -c 'from pathlib import Path; h=Path("site/index.html").read_text(); assert "data-range=\"1y\"" in h; assert "gold-chart-data" in h; assert "<details class=\"research-evidence\">" in h; assert "<h2>What Changed</h2>" not in h; print("static fallback contract: OK")'
```

预期：输出 `static fallback contract: OK`。

- [ ] **步骤 4：启动本地静态服务器并按 Browser 技能验收桌面端**

```bash
python3 -m http.server 8765 --directory site
```

使用内置浏览器打开 `http://127.0.0.1:8765/`，将 viewport 设为 `1440 × 900`，逐项验证：

1. 首屏顺序为价格与技术、主要驱动、最近三条变化；
2. 5 月中国购金约 `+9.95 吨`，6 月约 `+14.93 吨`，6 月柱更高；
3. 当前技术文字为“中期偏空 · 短期反弹 · 未形成完整排列”；
4. 区间按钮切换三个不同范围，`aria-pressed` 与可见 SVG 一致；
5. MA20、MA60、MA200 可独立显隐；
6. pointer 移动后 tooltip 日期与数值变化；
7. “研究依据”初始关闭，点击后展开关系检验和数据质量；
8. 浏览器控制台没有 error。

每次点击后读取对应按钮的 `aria-pressed`、当前可见 SVG 的 `data-range` 或 tooltip 文本，
不只依赖截图判断。

- [ ] **步骤 5：验收移动端和静态内容保留**

将 viewport 设为 `390 × 844`，验证无横向溢出，页面按“价格 → 技术 → 驱动 → 最近变化”堆叠。
随后刷新页面，确认默认一年 SVG 在脚本执行后仍是原始服务器 SVG；移除页面中的交互脚本节点后，
默认 SVG、驱动表、三条变化和研究内容仍存在且可读。

验收出现任何问题时，回到产生问题的任务，先增加能复现问题的自动断言，再做最小修复，
重新运行任务 6 的步骤 1—5；不在验收阶段直接做无测试的样式或逻辑修改。

- [ ] **步骤 6：让独立验收 subagent 只读复核最终差异**

验收 subagent 的任务限定为：对照设计稿逐项检查 `git diff 0965676..HEAD` 与未提交的
`site/index.html`，报告遗漏、口径偏差、可访问性问题和测试缺口；不得修改文件。
主代理逐条核实报告，不直接接受未经证据支持的结论。

- [ ] **步骤 7：运行最终证据命令并提交生成页面**

```bash
python3 -m unittest tests.test_build_site tests.test_refresh_wind_data tests.test_update_data_and_commit -v
python3 scripts/build_site.py
git diff --check -- data/market/official_reserves_manual.csv scripts/build_site.py tests/test_build_site.py site/index.html
git add site/index.html
git diff --cached --check
git commit -m "build: refresh gold decision dashboard" -m "Co-Authored-By: OpenAI <codex@openai.com>"
git status --short
```

预期：测试为 `OK`，两次 diff check 退出码为 0，提交成功；最终状态只保留既有未跟踪文件
`docs/pantheon_research_dashboard_notes.md`。不执行 push。
