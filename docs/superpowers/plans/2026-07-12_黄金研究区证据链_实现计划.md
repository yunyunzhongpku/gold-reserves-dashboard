# 黄金研究区证据链改版 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

修订:2026-07-13(按用户评审重写:撤自动分段改三态阈值参考带、复合 summary 改计数口径、钩子条补判定推导与 missing 分支、悬浮读数 JS 暂缓;详见 spec §13 修订记录)

**Goal:** 把「研究依据」从单一大折叠重组为 7 个独立证据单元(5 驱动 + 辅助观察 + 方法与数据),配反转轴主图、三态阈值参考带演变图、可推导的溯源钩子条与首屏联动。

**Architecture:** 全部改动收敛在 `scripts/build_site.py`(Python 预计算 + 字符串模板渲染 SVG/HTML)与 `tests/test_build_site.py`;JS/CSS 以内联方式写入生成模板。数据刷新与统计计算(变化对、滚动相关、前瞻收益)不动;渲染层重写,旧阶段表体系与三子块「综合相关」退役。

**Tech Stack:** Python 3 标准库 + openpyxl(现有);unittest;原生 HTML/SVG/JS,无外部依赖。

**设计依据:** `docs/superpowers/specs/2026-07-12_黄金研究区证据链_设计稿.md`(E1–E16 决策与各节合同;E14 悬浮读数已标暂缓)。

## Global Constraints

- 不新增任何依赖;不引入外部图表库;JS/CSS 内联在生成模板中。
- 三态词表只用「有效 / 偏弱 / 失效」,判定唯一入口是现有 `corr_tone`(顺预期 ≥0.35 有效,逆预期 ≥0.1 失效);不得新增相关性阈值;阈值带区间必须是 `corr_tone` 的精确翻译(有测试锁定,见 Task 1)。
- **无自动分段**:不存在 `segment_rolling_corr`、最小段长、段均值、regime 色条——若发现自己在实现这些,说明用错了计划版本,停下。
- **无悬浮读数 JS**:图表不输出 `chart_id`/JSON payload/`data-hover-*`;唯一的内联新 JS 是首屏「证据 →」联动展开。
- **不产出合成相关**:复合单元(资金与仓位)任何位置不得出现三子块相关系数的平均值。
- 钩子条阈值一律引用模块常量(`REAL_RATE_CHANGE_THRESHOLD` 等),不得在文案里硬编码数字字面量。
- 反转轴只作用于 `expected == "negative"` 的因子(实际利率、美元),轴题与图例必须含「已反转」;反转只翻几何,轴刻度文字始终是原始数值。
- 展开态内容列 `max-width: 680px`,图表 `max-width: 520px`,不满宽拉伸。
- 页面文案中文;commit 英文 conventional 风格,结尾加:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 与 `Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH`。
- `site/index.html` 是生成物:只能由 `python3 scripts/build_site.py` 再生,不得手工编辑。
- 每任务先写失败测试再实现;不为过检弱化、跳过、删除测试(退役功能的旧断言按任务中给出的映射改写,不是静默删除)。
- 完成全部任务后停在本地工作区,`git push` 需用户单独确认。

## 文件结构(全计划共 3 个文件被触碰)

| 文件 | 职责 | 变化 |
|---|---|---|
| `scripts/build_site.py` | 数据装配 + 全部渲染 | 新增 `corr_tone_bands`、钩子函数、证据单元渲染函数;重写 `make_corr_chart`、`make_dual_axis_chart`;`make_research_details` → `make_research_section`;删除三套关系卡模板、phases 体系与三子块综合相关;模板内新增少量 JS/CSS |
| `tests/test_build_site.py` | 行为测试 | 新增带/钩子/单元/联动测试类;按映射表改写旧断言 |
| `site/index.html` | 生成物 | 每任务结束由构建再生(不手改) |

关键行号锚点(实现时以 grep 校准,行号会随编辑漂移):常量区 51–67、`state_from_delta` ≈478、`corr_tone` ≈609、`rolling_corr` ≈675、`weak_periods_from_rolling` ≈759、`score_driver_layers` ≈831、各 `make_*_layer` 843–1037、`make_positioning_layer` ≈1040、`make_driver_rows` ≈1199、关系数据函数 1451–1830(`make_positioning_relationship` ≈1771,其中 `latest_corr` 平均在 ≈1800)、`read_dashboard_data` 的 staleness 写回 ≈1877、`make_sparkline` ≈2106、`make_bar_chart` ≈2188、`make_dual_axis_chart` ≈2227、`make_corr_chart` ≈2310、三卡模板+`make_relationship_section` 2380–2655、`make_driver_section` ≈2695、`make_research_details` ≈2806、内联 JS 在 `build_html` 模板尾部。

钩子条要引用的既有判定事实(Task 3 的依据,已核对源码):六驱动全部经 `state_from_delta` 或其变体判定——实际利率(21 观测变化,±0.05pct,下行支持)、美元(±0.5,下行支持)、通胀预期(±0.05pct,上行支持)、官方购金(月环比,容忍带 0,净买入支持)、ETF(21 观测,±3 吨,增持支持)、CFTC(4 周变化 ±15,000 张;156 周分位 >0.90 拥挤直接判压力);仓位层 state 由 ETF/CFTC 两票均值 ±0.5 合成(`combine_subsignal_states`,GVZ 不投票);`score_driver_layers` 剔除 `data_quality ∈ {missing, very-stale}` 的层,`read_dashboard_data` 统一按 `classify_staleness` 写回每层真实 `data_quality` 与 `lag_days`。

---

### Task 1: `make_corr_chart` 重写 —— 三态阈值参考带 + 中间刻度网格

**Files:**
- Modify: `scripts/build_site.py`(新增 `corr_tone_bands` 与两个色值常量;整体替换 `make_corr_chart`)
- Test: `tests/test_build_site.py`(新增 `EvidenceChartRenderTests` 类)

**Interfaces:**
- Consumes: `corr_tone`(现有,仅测试对照用);滚动相关行格式 `{"date": str, "_date": date, "corr": float|None}`。
- Produces:
  - `corr_tone_bands(expected) -> list[tuple]`:`[(下界, 上界, 态), ...]` 覆盖 [−1, +1],是 `corr_tone` 阈值的区间化精确翻译;
  - `make_corr_chart(series_defs, aria_label, width=720, height=180, limit=96, expected=None) -> str`。`expected=None` 时不画带,输出与旧版语义兼容(仍新增 ±0.5 中间刻度与网格);给定时铺三态水平底色带 + 带内右缘态名。

- [ ] **Step 1: 写失败测试**

在 `tests/test_build_site.py` 文件末尾新增(`date` 与 `build_site.shift_months` 在该测试文件已有引用先例;若文件头未导入 `date`,补 `from datetime import date`):

```python
class EvidenceChartRenderTests(unittest.TestCase):
    @staticmethod
    def _rolling(corr_values):
        rows = []
        day = date(2020, 1, 31)
        for corr in corr_values:
            rows.append({"date": day.isoformat(), "_date": day, "corr": corr})
            day = build_site.shift_months(day, 1)
        return rows

    def test_corr_tone_bands_translate_thresholds(self):
        self.assertEqual(
            build_site.corr_tone_bands("positive"),
            [(-1.0, -0.1, "失效"), (-0.1, 0.35, "偏弱"), (0.35, 1.0, "有效")])
        self.assertEqual(
            build_site.corr_tone_bands("negative"),
            [(-1.0, -0.35, "有效"), (-0.35, 0.1, "偏弱"), (0.1, 1.0, "失效")])
        self.assertEqual(
            [tone for _, _, tone in build_site.corr_tone_bands("absolute")],
            ["有效", "偏弱", "失效", "偏弱", "有效"])

    def test_bands_agree_with_corr_tone_at_probe_points(self):
        # 合同锁定:带只是 corr_tone 的可视化,任何探针点上两者不得矛盾。
        # 探针落在共享边界时属于相邻两带,断言 corr_tone 结果在其中即可。
        probes = [-0.99, -0.36, -0.35, -0.2, -0.1, -0.05, 0.0,
                  0.05, 0.1, 0.2, 0.35, 0.36, 0.99]
        for expected in ("positive", "negative", "absolute"):
            for probe in probes:
                tone = build_site.corr_tone(probe, expected=expected)
                hit = [t for lo, hi, t in build_site.corr_tone_bands(expected)
                       if lo <= probe <= hi]
                self.assertIn(tone, hit, msg=f"{expected} @ {probe}")

    def test_corr_chart_draws_bands_only_with_expected(self):
        rolling = self._rolling([0.6] * 30 + [0.0] * 30)
        series = [{"label": "24个月滚动相关", "rows": rolling, "color": "#237a57"}]
        with_bands = build_site.make_corr_chart(series, "测试滚动相关", expected="positive")
        without = build_site.make_corr_chart(series, "测试滚动相关")
        self.assertEqual(with_bands.count('class="tone-band"'), 3)
        self.assertEqual(with_bands.count('class="band-label"'), 3)
        for tone in ("有效", "偏弱", "失效"):
            self.assertIn(f">{tone}</text>", with_bands)
        self.assertNotIn("tone-band", without)
        self.assertIn('class="grid-line"', without)
        self.assertIn(">+0.5<", without)
        self.assertIn(">-0.5<", without)

    def test_absolute_bands_render_five_rects(self):
        rolling = self._rolling([0.6] * 30)
        html = build_site.make_corr_chart(
            [{"label": "x", "rows": rolling, "color": "#237a57"}], "t", expected="absolute")
        self.assertEqual(html.count('class="tone-band"'), 5)
        self.assertEqual(html.count(">有效</text>"), 2)

    def test_bands_paint_below_series_paths(self):
        rolling = self._rolling([0.6] * 30)
        html = build_site.make_corr_chart(
            [{"label": "x", "rows": rolling, "color": "#237a57"}], "t", expected="positive")
        self.assertLess(html.index('class="tone-band"'), html.index("<path"))
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_build_site.EvidenceChartRenderTests -v`
Expected: ERROR(`corr_tone_bands` 不存在;`make_corr_chart` 不接受 `expected`)。

- [ ] **Step 3: 实现**

在 `make_corr_chart` 定义上方新增:

```python
TONE_BAND_FILLS = {"有效": "#237a57", "偏弱": "#808080", "失效": "#b5543c"}
TONE_BAND_OPACITY = {"有效": 0.07, "偏弱": 0.05, "失效": 0.07}


def corr_tone_bands(expected):
    """corr_tone 阈值的区间化表述:[(下界, 上界, 态), ...],覆盖 [-1, +1]。"""
    if expected == "absolute":
        return [(-1.0, -0.35, "有效"), (-0.35, -0.1, "偏弱"), (-0.1, 0.1, "失效"),
                (0.1, 0.35, "偏弱"), (0.35, 1.0, "有效")]
    if expected == "negative":
        return [(-1.0, -0.35, "有效"), (-0.35, 0.1, "偏弱"), (0.1, 1.0, "失效")]
    return [(-1.0, -0.1, "失效"), (-0.1, 0.35, "偏弱"), (0.35, 1.0, "有效")]
```

用下面的完整实现替换原 `make_corr_chart` 函数体(签名到结尾):

```python
def make_corr_chart(series_defs, aria_label, width=720, height=180, limit=96,
                    expected=None):
    top_pad = 18
    bottom_pad = 34
    left_pad = 38
    right_pad = 16
    chart_width = width - left_pad - right_pad
    chart_height = height - top_pad - bottom_pad

    def y_for(value):
        clipped = max(-1, min(1, value))
        return top_pad + chart_height * (1 - (clipped + 1) / 2)

    zero_y = y_for(0)
    paths = []
    legends = []
    visible_points = []
    for series in series_defs:
        points = [row for row in series["rows"] if row.get("corr") is not None][-limit:]
        if len(points) < 2:
            continue
        visible_points.extend(points)
        coords = []
        for i, row in enumerate(points):
            x = left_pad + chart_width * i / (len(points) - 1)
            coords.append((x, y_for(row["corr"])))
        path = " ".join(f"{'M' if i == 0 else 'L'} {x:.1f} {y:.1f}" for i, (x, y) in enumerate(coords))
        dash = ' stroke-dasharray="5 4"' if series.get("dash") else ""
        paths.append(
            f'<path d="{path}" fill="none" stroke="{series["color"]}" stroke-width="2.4"{dash}></path>'
            f'<circle cx="{coords[-1][0]:.1f}" cy="{coords[-1][1]:.1f}" r="3.2" fill="{series["color"]}"></circle>'
        )
        legends.append(
            f'<span><i style="background:{series["color"]}"></i>{escape(series["label"])} {fmt_corr(points[-1]["corr"])}</span>'
        )

    if not paths:
        return '<div class="empty-chart">暂无足够滚动相关数据</div>'

    band_rects = []
    if expected:
        for lower, upper, tone in corr_tone_bands(expected):
            band_y = y_for(upper)
            band_height = y_for(lower) - y_for(upper)
            band_rects.append(
                f'<rect class="tone-band" x="{left_pad}" y="{band_y:.1f}" '
                f'width="{chart_width}" height="{band_height:.1f}" '
                f'fill="{TONE_BAND_FILLS[tone]}" fill-opacity="{TONE_BAND_OPACITY[tone]}"></rect>'
                f'<text class="band-label" x="{width - right_pad - 6}" '
                f'y="{band_y + band_height / 2 + 3:.1f}" text-anchor="end">{tone}</text>'
            )

    visible_points.sort(key=lambda row: row["_date"])
    first_label = visible_points[0]["date"][2:7]
    last_label = visible_points[-1]["date"][2:7]

    return f"""
    <div class="corr-wrap">
      <div class="corr-legend">{''.join(legends)}</div>
      <svg class="corr-chart" viewBox="0 0 {width} {height}" role="img" aria-label="{escape(aria_label)}">
        {''.join(band_rects)}
        <line x1="{left_pad}" y1="{y_for(0.5):.1f}" x2="{width - right_pad}" y2="{y_for(0.5):.1f}" class="grid-line"></line>
        <line x1="{left_pad}" y1="{y_for(-0.5):.1f}" x2="{width - right_pad}" y2="{y_for(-0.5):.1f}" class="grid-line"></line>
        <line x1="{left_pad}" y1="{zero_y:.1f}" x2="{width - right_pad}" y2="{zero_y:.1f}" class="zero-axis"></line>
        <line x1="{left_pad}" y1="{top_pad}" x2="{left_pad}" y2="{height - bottom_pad}" class="y-axis"></line>
        <line x1="{left_pad}" y1="{height - bottom_pad}" x2="{width - right_pad}" y2="{height - bottom_pad}" class="x-axis"></line>
        <text x="8" y="{top_pad + 4}" class="chart-label">+1</text>
        <text x="8" y="{y_for(0.5) + 4:.1f}" class="chart-label">+0.5</text>
        <text x="8" y="{zero_y + 4:.1f}" class="chart-label">0</text>
        <text x="8" y="{y_for(-0.5) + 4:.1f}" class="chart-label">-0.5</text>
        <text x="8" y="{height - bottom_pad}" class="chart-label">-1</text>
        <text x="{left_pad}" y="{height - 8}" class="chart-label">{first_label}</text>
        <text x="{width - right_pad}" y="{height - 8}" text-anchor="end" class="chart-label">{last_label}</text>
        {''.join(paths)}
      </svg>
    </div>
    """
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m unittest tests.test_build_site.EvidenceChartRenderTests -v` → `OK`。
全量回归:`python3 -m unittest tests.test_build_site -v` → 旧调用点不传 `expected`,输出向后兼容,应全绿。

- [ ] **Step 5: Commit**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: add corr_tone reference bands and gridlines to corr chart

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH"
```

---

### Task 2: `make_dual_axis_chart` 反转轴 + 刻度网格;`make_bar_chart` 补 Y 刻度

**Files:**
- Modify: `scripts/build_site.py`(整体替换 `make_dual_axis_chart`;`make_bar_chart` 插入刻度)
- Test: `tests/test_build_site.py`(`EvidenceChartRenderTests` 内追加 3 个测试)

**Interfaces:**
- Consumes: 关系字典的 `trend_rows`/`factor_key`/`gold_key`(现有格式)。
- Produces: `make_dual_axis_chart(rows, factor_key, factor_label, gold_key="gold_price", gold_label="黄金价格", width=720, height=220, limit=180, factor_color="#366b9f", gold_color="#b88728", invert_factor=False) -> str`。`invert_factor=True` 时右轴几何反向、轴题与 legend 标「已反转」,**右轴刻度文字仍为原始数值**(反转时最小值在顶端)。无 `chart_id`、无 JSON payload(E14 暂缓)。

- [ ] **Step 1: 写失败测试**

在 `EvidenceChartRenderTests` 类内追加(文件头若缺则补 `import re`、`from datetime import timedelta`):

```python
    @staticmethod
    def _trend_rows(count=80):
        rows = []
        day = date(2024, 1, 1)
        for i in range(count):
            rows.append({
                "date": day.isoformat(), "_date": day,
                "factor": 1.0 + i * 0.01, "gold_price": 2000.0 + i * 5,
            })
            day = day + timedelta(days=1)
        return rows

    def test_dual_axis_marks_inverted_axis_only_when_requested(self):
        rows = self._trend_rows()
        normal = build_site.make_dual_axis_chart(rows, "factor", "因子X")
        inverted = build_site.make_dual_axis_chart(
            rows, "factor", "因子X", invert_factor=True)
        self.assertNotIn("已反转", normal)
        self.assertIn("已反转", inverted)
        self.assertIn('class="grid-line"', normal)
        self.assertNotIn("data-hover-chart", normal)      # E14 暂缓,不得输出 hover 装置

    def test_dual_axis_inversion_flips_geometry_not_axis_values(self):
        rows = self._trend_rows()
        normal = build_site.make_dual_axis_chart(rows, "factor", "因子X")
        inverted = build_site.make_dual_axis_chart(
            rows, "factor", "因子X", invert_factor=True)

        def first_factor_y(html):
            match = re.search(r'<path d="M ([0-9.]+) ([0-9.]+)', html)
            return float(match.group(2))

        # 因子序列单调上升:正常轴首点在下(y 大),反转轴首点在上(y 小)
        self.assertGreater(first_factor_y(normal), first_factor_y(inverted))
        # 轴刻度是原始数值:正常轴顶端是最大值 1.79,反转轴顶端是最小值 1.00,
        # 两个数值在两种模式下都存在(只是位置对调),模板先渲染顶端刻度。
        for html in (normal, inverted):
            self.assertIn(">1.79<", html)
            self.assertIn(">1.00<", html)
        self.assertLess(normal.index(">1.79<"), normal.index(">1.00<"))
        self.assertLess(inverted.index(">1.00<"), inverted.index(">1.79<"))

    def test_bar_chart_shows_y_axis_scale(self):
        rows = [
            {"date": f"2025-{month:02d}-28", "_date": date(2025, month, 28), "change": value}
            for month, value in [(1, 10.0), (2, -5.0), (3, 15.0)]
        ]
        html = build_site.make_bar_chart(rows, "change", "测试柱状图", "china-bar")
        self.assertIn(">+15<", html)
        self.assertIn(">0<", html)
        self.assertIn(">-15<", html)
```

(`fmt_axis_value(1.79)` 按现实现输出 `1.79`、`fmt_axis_value(15.0)` 输出 `15`;若实测格式不同,以 `fmt_axis_value` 实际行为为准修正断言,不改函数。断言用 `>值<` 形式绑定刻度文本节点,金价刻度为千位数不会误命中。)

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_build_site.EvidenceChartRenderTests -v`
Expected: 新增 3 个测试 FAIL/ERROR(不接受 `invert_factor`;柱状图无刻度文本)。

- [ ] **Step 3: 实现**

(a) 用下面完整实现替换 `make_dual_axis_chart`:

```python
def make_dual_axis_chart(
    rows,
    factor_key,
    factor_label,
    gold_key="gold_price",
    gold_label="黄金价格",
    width=720,
    height=220,
    limit=180,
    factor_color="#366b9f",
    gold_color="#b88728",
    invert_factor=False,
):
    points = [
        row for row in rows
        if row.get(factor_key) is not None and row.get(gold_key) is not None
    ][-limit:]
    if len(points) < 2:
        return '<div class="empty-chart">暂无足够双轴走势数据</div>'

    left_pad = 56
    right_pad = 66
    top_pad = 22
    bottom_pad = 32
    chart_width = width - left_pad - right_pad
    chart_height = height - top_pad - bottom_pad

    factor_values = [row[factor_key] for row in points]
    gold_values = [row[gold_key] for row in points]
    factor_min, factor_max = min(factor_values), max(factor_values)
    gold_min, gold_max = min(gold_values), max(gold_values)
    factor_span = factor_max - factor_min or 1
    gold_span = gold_max - gold_min or 1

    def x_for(index):
        return left_pad + chart_width * index / (len(points) - 1)

    def gold_y(value):
        return top_pad + chart_height * (1 - (value - gold_min) / gold_span)

    def factor_y(value):
        ratio = (value - factor_min) / factor_span
        if invert_factor:
            return top_pad + chart_height * ratio
        return top_pad + chart_height * (1 - ratio)

    factor_coords = [(x_for(i), factor_y(row[factor_key])) for i, row in enumerate(points)]
    gold_coords = [(x_for(i), gold_y(row[gold_key])) for i, row in enumerate(points)]
    factor_path = " ".join(
        f"{'M' if i == 0 else 'L'} {x:.1f} {y:.1f}" for i, (x, y) in enumerate(factor_coords))
    gold_path = " ".join(
        f"{'M' if i == 0 else 'L'} {x:.1f} {y:.1f}" for i, (x, y) in enumerate(gold_coords))
    first_label = points[0]["date"][2:7]
    last_label = points[-1]["date"][2:7]

    mid_y = top_pad + chart_height / 2
    grid_ys = [top_pad + chart_height * ratio for ratio in (0.25, 0.5, 0.75)]
    grid_lines = "".join(
        f'<line x1="{left_pad}" y1="{y:.1f}" x2="{width - right_pad}" y2="{y:.1f}" class="grid-line"></line>'
        for y in grid_ys
    )
    factor_axis_top = factor_min if invert_factor else factor_max
    factor_axis_bottom = factor_max if invert_factor else factor_min
    factor_axis_mid = (factor_min + factor_max) / 2
    invert_note = "(已反转)" if invert_factor else ""

    return f"""
    <div class="dual-axis-wrap">
      <div class="dual-axis-legend">
        <span><i style="background:{factor_color}"></i>{escape(factor_label)}{invert_note} {fmt_axis_value(points[-1][factor_key])}</span>
        <span><i style="background:{gold_color}"></i>{escape(gold_label)} {fmt_axis_value(points[-1][gold_key])}</span>
      </div>
      <svg class="dual-axis-chart" viewBox="0 0 {width} {height}" role="img" aria-label="{escape(factor_label)}与{escape(gold_label)}双轴走势{invert_note}">
        {grid_lines}
        <line x1="{left_pad}" y1="{top_pad}" x2="{left_pad}" y2="{height - bottom_pad}" class="left-axis"></line>
        <line x1="{width - right_pad}" y1="{top_pad}" x2="{width - right_pad}" y2="{height - bottom_pad}" class="right-axis"></line>
        <line x1="{left_pad}" y1="{height - bottom_pad}" x2="{width - right_pad}" y2="{height - bottom_pad}" class="x-axis"></line>
        <text x="6" y="{top_pad + 4}" class="chart-label">{fmt_axis_value(gold_max)}</text>
        <text x="6" y="{mid_y + 4:.1f}" class="chart-label">{fmt_axis_value((gold_min + gold_max) / 2)}</text>
        <text x="6" y="{height - bottom_pad}" class="chart-label">{fmt_axis_value(gold_min)}</text>
        <text x="{width - 6}" y="{top_pad + 4}" text-anchor="end" class="chart-label">{fmt_axis_value(factor_axis_top)}</text>
        <text x="{width - 6}" y="{mid_y + 4:.1f}" text-anchor="end" class="chart-label">{fmt_axis_value(factor_axis_mid)}</text>
        <text x="{width - 6}" y="{height - bottom_pad}" text-anchor="end" class="chart-label">{fmt_axis_value(factor_axis_bottom)}</text>
        <path d="{factor_path}" fill="none" stroke="{factor_color}" stroke-width="2.3"></path>
        <path d="{gold_path}" fill="none" stroke="{gold_color}" stroke-width="2.3"></path>
        <circle cx="{factor_coords[-1][0]:.1f}" cy="{factor_coords[-1][1]:.1f}" r="3.1" fill="{factor_color}"></circle>
        <circle cx="{gold_coords[-1][0]:.1f}" cy="{gold_coords[-1][1]:.1f}" r="3.1" fill="{gold_color}"></circle>
        <text x="{left_pad}" y="{height - 8}" class="chart-label">{first_label}</text>
        <text x="{width - right_pad}" y="{height - 8}" text-anchor="end" class="chart-label">{last_label}</text>
      </svg>
    </div>
    """
```

注意:左轴顺序改为金价(旧版左轴是因子)。这是有意变化——证据单元的叙事主体是金价,与 spec §6.1「左轴金价」一致;旧调用点(仓位子块等)随 Task 4/5 重组,不需要兼容旧轴序。

(b) `make_bar_chart` 里,把

```python
      <line x1="30" y1="24" x2="30" y2="210" class="y-axis"></line>
      <line x1="30" y1="{mid_y}" x2="{svg_width - 20}" y2="{mid_y}" class="x-axis"></line>
```

替换为

```python
      <line x1="30" y1="24" x2="30" y2="210" class="y-axis"></line>
      <line x1="30" y1="{mid_y}" x2="{svg_width - 20}" y2="{mid_y}" class="x-axis"></line>
      <text x="2" y="34" class="chart-label">+{fmt_axis_value(max_abs)}</text>
      <text x="2" y="{mid_y + 4}" class="chart-label">0</text>
      <text x="2" y="196" class="chart-label">-{fmt_axis_value(max_abs)}</text>
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m unittest tests.test_build_site.EvidenceChartRenderTests -v` → `OK`。
全量:`python3 -m unittest tests.test_build_site -v` → `OK`(旧调用点不传新参,输出兼容;`test_charts_show_axes_and_underlying_series` 的 `right-axis`/`dual-axis-chart`/「黄金价格」断言不受轴序调换影响)。

- [ ] **Step 5: Commit**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: add inverted axis and gridlines to dual-axis chart

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH"
```

---

### Task 3: 溯源钩子条 —— 判定推导文案 + 未计入姿态分支(E15)

**Files:**
- Modify: `scripts/build_site.py`(在 `make_relationship_section` 之前新增 3 个函数)
- Test: `tests/test_build_site.py`(新增 `EvidenceHookTests` 类)

**Interfaces:**
- Consumes: layer 字典既有键 `id`/`state`/`data_quality`/`lag_days`/`change_label`/`latest`/`sub_states`(positioning);模块常量 `DRIVER_DAILY_LOOKBACK`、`REAL_RATE_CHANGE_THRESHOLD`、`DOLLAR_CHANGE_THRESHOLD`、`INFLATION_CHANGE_THRESHOLD`、`ETF_CHANGE_THRESHOLD_TONNES`、`COT_CHANGE_LOOKBACK_WEEKS`、`COT_CHANGE_THRESHOLD_CONTRACTS`、`COT_CROWDING_PERCENTILE`、`POSITIONING_AVERAGE_VOTE_THRESHOLD`;`STATE_LABELS`、`QUALITY_LABELS`、`state_badge`(现有)。
- Produces:
  - `evidence_hook_line(layer) -> str`:单驱动判定推导句或「未计入姿态」句;
  - `positioning_hook_lines(layer) -> list[str]`:ETF / CFTC / 合成 三行;
  - `make_evidence_hook(layer) -> str`:`<div class="evidence-hook">` 完整渲染。
- 「未计入姿态」的触发条件必须与 `score_driver_layers` 的剔除条件一致:`data_quality ∈ {missing, very-stale}` 或 `state ∉ {supportive, neutral, headwind}`。不重复实现投票,只共享条件。

- [ ] **Step 1: 写失败测试**

```python
class EvidenceHookTests(unittest.TestCase):
    def test_real_rate_hook_explains_threshold_and_verdict(self):
        layer = {"id": "real_rate", "state": "headwind", "data_quality": "fresh",
                 "change": 0.18, "change_label": "+0.18pct", "lag_days": 1}
        html = build_site.make_evidence_hook(layer)
        self.assertIn("+0.18pct", html)          # 变化值
        self.assertIn("±0.05pct", html)          # REAL_RATE_CHANGE_THRESHOLD
        self.assertIn("判『压力』", html)          # 判定结论
        self.assertIn("#evidence-method", html)  # 完整规则链接

    def test_reserve_hook_uses_monthly_wording(self):
        layer = {"id": "official_reserves", "state": "supportive", "data_quality": "fresh",
                 "change": 5.0, "change_label": "+5.0 吨", "lag_days": 10}
        html = build_site.make_evidence_hook(layer)
        self.assertIn("+5.0 吨", html)
        self.assertIn("净买入判支持", html)
        self.assertIn("判『支持』", html)

    def test_missing_layer_hook_says_excluded_from_posture(self):
        layer = {"id": "dollar", "state": "missing", "data_quality": "missing",
                 "change": None, "change_label": "—", "lag_days": None}
        html = build_site.make_evidence_hook(layer)
        self.assertIn("未计入首屏姿态", html)
        self.assertNotIn("判『", html)

    def test_very_stale_layer_hook_reports_lag_days(self):
        layer = {"id": "real_rate", "state": "headwind", "data_quality": "very-stale",
                 "change": 0.18, "change_label": "+0.18pct", "lag_days": 30}
        html = build_site.make_evidence_hook(layer)
        self.assertIn("严重滞后", html)
        self.assertIn("30 天", html)
        self.assertIn("未计入首屏姿态", html)

    def test_positioning_hook_lists_sub_rules_and_vote(self):
        layer = {"id": "positioning_technical", "state": "neutral", "data_quality": "fresh",
                 "sub_states": {"etf": "supportive", "cot": "headwind"},
                 "latest": {"etf_change": 12.4, "managed_money_net_change": -18000},
                 "lag_days": 0}
        html = build_site.make_evidence_hook(layer)
        self.assertIn("±3 吨", html)             # ETF_CHANGE_THRESHOLD_TONNES
        self.assertIn("±15,000 张", html)        # COT_CHANGE_THRESHOLD_CONTRACTS
        self.assertIn("90%", html)               # COT_CROWDING_PERCENTILE
        self.assertIn("GVZ 不投票", html)
        self.assertIn("判『中性』", html)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_build_site.EvidenceHookTests -v`
Expected: ERROR(`make_evidence_hook` 不存在)。

- [ ] **Step 3: 实现**

```python
def evidence_hook_line(layer):
    """单驱动的判定推导句;数据被剔除时如实说明未计入姿态。"""
    quality = layer.get("data_quality")
    state = layer.get("state")
    if quality in ("missing", "very-stale") or state not in ("supportive", "neutral", "headwind"):
        lag = layer.get("lag_days")
        lag_part = f"（滞后 {lag} 天）" if lag is not None else ""
        return f"数据{QUALITY_LABELS.get(quality, '缺失')}{lag_part}，该驱动本期未计入首屏姿态。"
    rules = {
        "real_rate": f"容忍带 ±{REAL_RATE_CHANGE_THRESHOLD}pct，利率下行超带判支持、上行超带判压力",
        "dollar": f"容忍带 ±{DOLLAR_CHANGE_THRESHOLD}，美元走弱超带判支持、走强超带判压力",
        "official_reserves": "净买入判支持、净卖出判压力、零变动判中性",
        "inflation_expectation": f"容忍带 ±{INFLATION_CHANGE_THRESHOLD}pct，通胀预期上行超带判支持、回落超带判压力",
    }
    window = (
        "最新月环比购金" if layer["id"] == "official_reserves"
        else f"近 {DRIVER_DAILY_LOOKBACK} 个有效观测变化"
    )
    return (
        f"{window} {layer.get('change_label') or '—'}，{rules[layer['id']]}"
        f" → 判『{STATE_LABELS[state]}』。"
    )


def positioning_hook_lines(layer):
    latest = layer["latest"]
    sub = layer["sub_states"]
    return [
        (f"ETF：近 {DRIVER_DAILY_LOOKBACK} 个观测持仓变化 "
         f"{fmt_signed(latest.get('etf_change'), suffix=' 吨')}，"
         f"容忍带 ±{ETF_CHANGE_THRESHOLD_TONNES:g} 吨 → {STATE_LABELS.get(sub['etf'], '缺失')}"),
        (f"CFTC：近 {COT_CHANGE_LOOKBACK_WEEKS} 周净多变化 "
         f"{fmt_signed_integer(latest.get('managed_money_net_change'), suffix=' 张')}，"
         f"容忍带 ±{COT_CHANGE_THRESHOLD_CONTRACTS:,} 张；三年分位超过 "
         f"{COT_CROWDING_PERCENTILE:.0%} 视为拥挤、直接判压力 → {STATE_LABELS.get(sub['cot'], '缺失')}"),
        (f"合成：两票均值 ≥ +{POSITIONING_AVERAGE_VOTE_THRESHOLD:g} 判支持、"
         f"≤ -{POSITIONING_AVERAGE_VOTE_THRESHOLD:g} 判压力，其余中性；GVZ 不投票"
         f" → 判『{STATE_LABELS[layer['state']]}』。"),
    ]


def make_evidence_hook(layer):
    scoring = (
        layer.get("state") in ("supportive", "neutral", "headwind")
        and layer.get("data_quality") not in ("missing", "very-stale")
    )
    if layer["id"] == "positioning_technical" and scoring:
        body = "<br>".join(escape(line) for line in positioning_hook_lines(layer))
    else:
        body = escape(evidence_hook_line(layer))
    return f"""
      <div class="evidence-hook">
        <strong>首屏当前：{state_badge(layer['state'])}</strong> · {body}
        <a href="#evidence-method">完整规则见方法与数据 →</a>
      </div>
    """
```

实现前先 grep 确认 `state_badge`、`fmt_signed`、`fmt_signed_integer` 的实际签名与输出(断言中的「压力」来自 `STATE_LABELS["headwind"]`)。若 `state_badge(state)` 对 `missing` 输出的徽章文字不是「缺失」,以实际输出调整测试预期,不改 `STATE_LABELS`。

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m unittest tests.test_build_site.EvidenceHookTests -v` → `OK`;全量 → `OK`(新函数尚无调用方,零影响)。

- [ ] **Step 5: Commit**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: add evidence hook with threshold derivation and exclusion note

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH"
```

---

### Task 4: 证据单元组件(summary 行 / 关系图块 / 标准单元)

**Files:**
- Modify: `scripts/build_site.py`(在 Task 3 函数之后新增 3 个函数)
- Test: `tests/test_build_site.py`(新增 `EvidenceUnitTests` 类)

**Interfaces:**
- Consumes: Task 1–3 的全部产出;`tone_badge`、`quality_badge`、`make_sparkline`、`fmt_corr`(现有)。
- Produces:
  - `make_evidence_summary(title, source_note, badge_html, spark_html, corr_html, quality_html) -> str`(`<summary class="evidence-summary">`)
  - `make_evidence_relationship_block(relationship, corr_label, chart_limit) -> str`(主图 + 演变图;复合/辅助单元复用它渲染子块)
  - `make_evidence_unit(unit_id, title, source_note, relationship, layer, chart_limit, spark_limit, corr_label, extra_sections="") -> str`(完整 `<details class="evidence-unit" id="{unit_id}">`)
- 反转规则:块内 `invert_factor = relationship["expected"] == "negative"`,唯一判定点;演变图 `expected=relationship["expected"]` 直通 `make_corr_chart`。
- 单因子 summary 显示自身 `latest_corr`(口径干净);复合单元的 summary 由 Task 5 的专用函数生成,**不走本函数的 corr 位**。

- [ ] **Step 1: 写失败测试**

```python
class EvidenceUnitTests(unittest.TestCase):
    @staticmethod
    def _relationship(expected="negative"):
        rows = []
        day = date(2023, 1, 1)
        rolling = []
        for i in range(400):
            rows.append({
                "date": day.isoformat(), "_date": day,
                "real_rate": 1.0 + i * 0.002, "gold_price": 2000.0 + i * 3,
            })
            if i >= 100:
                rolling.append({
                    "date": day.isoformat(), "_date": day,
                    "corr": -0.6 if i < 250 else 0.3,
                })
            day = day + timedelta(days=1)
        return {
            "id": "real_rate", "name": "实际利率", "expected": expected,
            "latest_corr": rolling[-1]["corr"],
            "latest_tone": build_site.corr_tone(rolling[-1]["corr"], expected=expected),
            "rolling_corr": rolling, "trend_rows": rows,
            "factor_key": "real_rate", "factor_label": "实际利率",
            "gold_key": "gold_price",
            "read": "实际利率是持金机会成本,预期负相关。",
        }

    @staticmethod
    def _layer():
        return {"id": "real_rate", "state": "headwind", "data_quality": "fresh",
                "change": 0.18, "change_label": "+0.18pct", "lag_days": 1}

    def test_evidence_unit_assembles_summary_hook_charts_and_bands(self):
        html = build_site.make_evidence_unit(
            "evidence-real-rate", "实际利率", "FRED DFII10",
            self._relationship(), self._layer(),
            chart_limit=180, spark_limit=504, corr_label="252日滚动相关(中期)",
        )
        self.assertIn('<details class="evidence-unit" id="evidence-real-rate">', html)
        self.assertIn('class="evidence-summary"', html)
        self.assertIn("FRED DFII10", html)
        self.assertIn('class="evidence-hook"', html)
        self.assertIn("±0.05pct", html)                  # 钩子含阈值推导
        self.assertIn("已反转", html)                     # expected=negative → 主图反转
        self.assertIn('class="tone-band"', html)          # 演变图阈值带
        self.assertIn('class="evidence-spark"', html)
        self.assertIn('class="sparkline"', html)
        self.assertIn("滚动相关", html)
        self.assertIn('class="evidence-body"', html)
        self.assertIn("重叠样本", html)                    # 尾注 n 说明(E9)

    def test_positive_expected_unit_does_not_invert(self):
        relationship = self._relationship(expected="positive")
        html = build_site.make_evidence_unit(
            "evidence-x", "测试因子", "来源", relationship, self._layer(),
            chart_limit=84, spark_limit=24, corr_label="24个月滚动相关",
        )
        self.assertNotIn("已反转", html)

    def test_unit_without_layer_has_no_hook(self):
        html = build_site.make_evidence_unit(
            "evidence-aux", "辅助", "来源", self._relationship(), None,
            chart_limit=180, spark_limit=504, corr_label="252日滚动相关(中期)",
        )
        self.assertNotIn("evidence-hook", html)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_build_site.EvidenceUnitTests -v`
Expected: ERROR(`make_evidence_unit` 不存在)。

- [ ] **Step 3: 实现**

```python
def make_evidence_summary(title, source_note, badge_html, spark_html, corr_html, quality_html):
    return f"""
      <summary class="evidence-summary">
        <span class="evidence-caret" aria-hidden="true"></span>
        <span class="evidence-title"><strong>{escape(title)}</strong><small>{escape(source_note)}</small></span>
        {badge_html}
        <span class="evidence-spark">{spark_html}</span>
        <span class="evidence-corr">{corr_html}</span>
        <span class="evidence-quality">{quality_html}</span>
      </summary>
    """


def make_evidence_relationship_block(relationship, corr_label, chart_limit):
    expected = relationship["expected"]
    dual = make_dual_axis_chart(
        relationship["trend_rows"],
        relationship["factor_key"],
        relationship["factor_label"],
        relationship["gold_key"],
        limit=chart_limit,
        invert_factor=expected == "negative",
    )
    series = [{"label": corr_label, "rows": relationship["rolling_corr"], "color": "#237a57"}]
    if relationship.get("short_term"):
        series.append({
            "label": relationship["short_term"]["label"],
            "rows": relationship["short_term"]["rolling_corr"],
            "color": "#366b9f",
            "dash": True,
        })
    corr = make_corr_chart(
        series,
        f"{relationship['name']}与黄金收益滚动相关及三态阈值带",
        limit=chart_limit,
        expected=expected,
    )
    return f"""
      <div class="evidence-chart-block">
        <h4>主关系走势</h4>
        {dual}
        <h4>关系演变</h4>
        {corr}
      </div>
    """


def make_evidence_unit(unit_id, title, source_note, relationship, layer,
                       chart_limit, spark_limit, corr_label, extra_sections=""):
    spark = make_sparkline(
        relationship["rolling_corr"], "corr",
        color="#237a57", width=64, height=16, limit=spark_limit)
    summary = make_evidence_summary(
        title,
        source_note,
        tone_badge(relationship["latest_tone"]),
        spark,
        f"滚动相关 <strong>{fmt_corr(relationship['latest_corr'])}</strong>",
        f'<span class="quality">{quality_badge(layer["data_quality"])}</span>' if layer else "",
    )
    hook = make_evidence_hook(layer) if layer else ""
    block = make_evidence_relationship_block(relationship, corr_label, chart_limit)
    return f"""
    <details class="evidence-unit" id="{unit_id}">
      {summary}
      <div class="evidence-body">
        {hook}
        <p class="evidence-read">{escape(relationship['read'])}</p>
        {block}
        {extra_sections}
        <p class="evidence-footnote">徽章与阈值带按「{escape(corr_label)}」以现有阈值(顺预期 ≥0.35 有效、逆预期 ≥0.1 失效)判定;滚动窗为重叠样本,n 即窗口长度;阈值、窗口与已知局限见「方法与数据」。</p>
      </div>
    </details>
    """
```

说明:(1) `short_term` 键为日频双窗关系(`make_two_horizon_relationship` 输出)所有,月频因子没有——`get` 缺省即正确降级,实现时 grep 现有关系字典确认键名。(2) `make_sparkline` 输出含 y 轴与首尾标签,在 64×16 下文字会溢出——本任务先接受(结构正确性优先),Task 7 的 CSS 用 `.evidence-spark svg text { display: none; }` 隐藏 sparkline 内文字、只留线形,不改 Python。

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m unittest tests.test_build_site.EvidenceUnitTests -v` → `OK`;全量 → `OK`。

- [ ] **Step 5: Commit**

```bash
git add scripts/build_site.py tests/test_build_site.py
git commit -m "feat: add evidence unit components with summary row

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH"
```

---

### Task 5: 研究区总组装 + 首屏驱动行锚链 + 旧断言映射改写

**Files:**
- Modify: `scripts/build_site.py`(新增 `make_research_section`、三个专用单元函数与 `tone_counts_text`;`make_driver_section` 加锚链;`build_html` 换调用)
- Modify: `tests/test_build_site.py`(新增 `ResearchSectionTests`;按映射表改写旧断言)

**Interfaces:**
- Consumes: Task 3–4 全部;`dashboard` 字典(`relationships`/`layers`/`reserve_rows`/`valuation` 等,现有);`make_bar_chart`(现有)。
- Produces: `make_research_section(dashboard) -> str`(`<section class="research-section" id="research">` 含 7 个 `<details class="evidence-unit">`,id 见 spec §4);`DRIVER_EVIDENCE_TARGETS`(dict);`tone_counts_text(tones) -> str`(复合 summary 计数,E16);驱动行内 `<a class="evidence-link" href="#evidence-…" data-evidence-target="evidence-…">证据 →</a>`。`build_html` 中 `make_research_details(dashboard)` 的调用点改为 `make_research_section(dashboard)`(旧函数本任务保留不删,Task 6 删)。

- [ ] **Step 1: 写失败测试**

`TODAY` 值:先 `grep -n "TODAY = date(" tests/test_build_site.py`,与现有集成测试类完全一致(避免两套快照日期)。

```python
class ResearchSectionTests(unittest.TestCase):
    TODAY = date(2026, 7, 10)   # 以现有集成测试的 TODAY 为准

    def test_tone_counts_text_orders_and_skips_zero(self):
        self.assertEqual(
            build_site.tone_counts_text(["有效", "失效", "有效"]), "2 有效 · 1 失效")
        self.assertEqual(build_site.tone_counts_text(["偏弱"]), "1 偏弱")
        self.assertEqual(build_site.tone_counts_text(["样本不足"]), "样本不足")

    def test_research_section_exposes_seven_evidence_units(self):
        dashboard = build_site.read_dashboard_data(today=self.TODAY)
        html = build_site.build_html(dashboard)
        self.assertEqual(html.count('<details class="evidence-unit"'), 7)
        for anchor in [
            "evidence-real-rate", "evidence-dollar", "evidence-reserve",
            "evidence-inflation", "evidence-positioning",
            "evidence-auxiliary", "evidence-method",
        ]:
            self.assertIn(f'id="{anchor}"', html)
        self.assertNotIn('<details class="research-evidence">', html)
        self.assertNotIn("阶段表现", html)
        self.assertNotIn('class="phase-table"', html)
        self.assertNotIn('class="metric-strip"', html)
        self.assertIn("研究依据", html)
        self.assertIn("不参与首页姿态", html)          # 辅助观察提示
        self.assertIn("官方购金历史", html)             # 柱状图并入购金单元
        self.assertIn("数据质量与来源", html)
        self.assertIn("计算方法与已知局限", html)

    def test_driver_rows_link_to_evidence_units(self):
        dashboard = build_site.read_dashboard_data(today=self.TODAY)
        html = build_site.build_html(dashboard)
        self.assertIn(
            'href="#evidence-real-rate" data-evidence-target="evidence-real-rate"', html)
        self.assertIn(
            'href="#evidence-reserve" data-evidence-target="evidence-reserve"', html)
        self.assertEqual(html.count('data-evidence-target="evidence-positioning"'), 2)

    def test_positioning_summary_shows_tone_counts_not_average(self):
        dashboard = build_site.read_dashboard_data(today=self.TODAY)
        html = build_site.build_html(dashboard)
        start = html.index('id="evidence-positioning"')
        body_start = html.index('<div class="evidence-body"', start)
        summary_html = html[start:body_start]
        self.assertIn('class="evidence-count"', summary_html)
        self.assertRegex(summary_html, r"\d+ (有效|偏弱|失效)")
        self.assertNotIn("滚动相关", summary_html)      # summary 无任何合成相关值
        self.assertNotIn("sparkline", summary_html)     # summary 无 sparkline(E16)

    def test_gvz_sub_block_is_not_inverted_and_positioning_has_sub_blocks(self):
        dashboard = build_site.read_dashboard_data(today=self.TODAY)
        html = build_site.build_html(dashboard)
        start = html.index('id="evidence-positioning"')
        end = html.index('id="evidence-auxiliary"')
        positioning_html = html[start:end]
        self.assertIn("ETF 持仓", positioning_html)
        self.assertIn("Managed Money 净多", positioning_html)
        self.assertIn("GVZ 波动率", positioning_html)
        gvz_part = positioning_html[positioning_html.index("GVZ 波动率"):]
        self.assertNotIn("已反转", gvz_part)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_build_site.ResearchSectionTests -v`
Expected: FAIL/ERROR(仍输出旧 `research-evidence` 结构;`tone_counts_text` 不存在)。

- [ ] **Step 3: 实现**

(a) 在 `make_research_details` 之前新增:

```python
DRIVER_EVIDENCE_TARGETS = {
    "real_rate": "evidence-real-rate",
    "dollar": "evidence-dollar",
    "official_reserves": "evidence-reserve",
    "inflation_expectation": "evidence-inflation",
    "etf": "evidence-positioning",
    "cot": "evidence-positioning",
}


def tone_counts_text(tones):
    parts = []
    for word in ("有效", "偏弱", "失效"):
        count = tones.count(word)
        if count:
            parts.append(f"{count} {word}")
    return " · ".join(parts) if parts else "样本不足"


def make_positioning_evidence_unit(relationship, layer):
    sub_blocks = []
    for sub_metric in relationship["sub_metrics"]:
        block = make_evidence_relationship_block(sub_metric, "52期滚动相关", 120)
        sub_blocks.append(
            f'<div class="evidence-sub-block"><h3>{escape(sub_metric["name"])} '
            f'{tone_badge(sub_metric["latest_tone"])}'
            f'<small>滚动相关 {fmt_corr(sub_metric["latest_corr"])}</small></h3>{block}</div>'
        )
    counts = tone_counts_text([sub["latest_tone"] for sub in relationship["sub_metrics"]])
    summary = make_evidence_summary(
        "资金与仓位", "ETF / CFTC / GVZ",
        f'<span class="evidence-count">{escape(counts)}</span>',
        "", "",
        f'<span class="quality">{quality_badge(layer["data_quality"])}</span>',
    )
    return f"""
    <details class="evidence-unit" id="evidence-positioning">
      {summary}
      <div class="evidence-body">
        {make_evidence_hook(layer)}
        <p class="evidence-read">{escape(relationship['read'])}</p>
        {''.join(sub_blocks)}
        <p class="evidence-footnote">三个子指标分别判定:ETF、CFTC 看方向,GVZ 看绝对相关强弱(无方向、不作轴反转);口径不同,不产出三者的合成相关值。阈值与窗口见「方法与数据」。</p>
      </div>
    </details>
    """
```

(b) `make_auxiliary_evidence_unit(relationships, valuation)`:结构与 7-12 版一致——`aux_defs = [("price_trend", 180, "252日滚动相关(中期)"), ("epu", 84, "24个月滚动相关"), ("gpr", 84, "24个月滚动相关")]`(无 `min_len`),逐个 `make_evidence_relationship_block(relationship, corr_label, chart_limit)` 包 `evidence-sub-block`;单元顶部 `<p class="evidence-note">以下指标不参与首页姿态计分,仅作背景观察。</p>`;末尾并入现有长期估值快照文案(从 `make_research_details` 平移 `valuation` 相关行);summary 用 `make_evidence_summary("辅助观察", "价格趋势 / EPU / GPR", '<span class="evidence-count">不计分</span>', "", "", "")`。

(c) `make_method_evidence_unit(dashboard)`:把现 `make_research_details` 中「数据质量与来源」与「计算方法与已知局限」两个 `<section>` 的内部内容原样平移(`quality_rows`/`positioning_read`/`scoring_summary`/`method_contract_rows` 的计算逻辑搬入,变量名不变,**所有既有文案字符串逐字保留**;估值行已移辅助单元,删去该行),包进 `<details class="evidence-unit" id="evidence-method">`,summary 用 `make_evidence_summary("方法与数据", "口径 · 阈值 · 来源 · 已知局限", "", "", "", "")`。原 `<ul>` 14 条 li 全部保留,并追加 2 条新 li:

```html
<li>关系演变图：滚动相关折线叠加三态阈值参考带（顺预期 ≥0.35 有效、逆预期 ≥0.1 失效、其余偏弱；GVZ 按绝对值），带边界即现有 corr_tone 阈值，不做历史分段。</li>
<li>滚动窗口为重叠样本：n 为观测数、有效自由度低于 n，相关值只是描述统计，不宣称显著性。</li>
```

(d) `make_research_section(dashboard)`:与 7-12 版结构一致——收集 `relationships`/`layers` 字典,购金单元 `extra_sections` 并入中国/全球环比柱状图(`make_bar_chart`,含数据日期与来源行),然后按 spec §4 顺序组装 7 个单元:

```python
    units = [
        make_evidence_unit(
            "evidence-real-rate", "实际利率", "FRED DFII10",
            relationships["real_rate"], layers["real_rate"],
            180, 504, "252日滚动相关(中期)"),
        make_evidence_unit(
            "evidence-dollar", "美元", "Wind USDX.FX",
            relationships["dollar"], layers["dollar"],
            180, 504, "252日滚动相关(中期)"),
        make_evidence_unit(
            "evidence-reserve", "官方购金", "SAFE · 月频",
            relationships["central_bank_purchases"], layers["official_reserves"],
            84, 24, "24个月滚动相关",
            extra_sections=reserve_extra),
        make_evidence_unit(
            "evidence-inflation", "通胀预期", "FRED T10YIE",
            relationships["inflation_expectation"], layers["inflation_expectation"],
            180, 504, "252日滚动相关(中期)"),
        make_positioning_evidence_unit(
            relationships["positioning_technical"], layers["positioning_technical"]),
        make_auxiliary_evidence_unit(dashboard["relationships"], dashboard["valuation"]),
        make_method_evidence_unit(dashboard),
    ]
    return f"""
    <section class="research-section" id="research">
      <div class="section-heading"><h2>研究依据</h2><span>五组驱动证据 · 辅助观察 · 方法与数据</span></div>
      {''.join(units)}
    </section>
    """
```

(`relationships` 各键名与 `reserve_extra` 组装照 7-12 版;关系 id 以 `make_relationships` 实际输出为准 grep 校准,例如央行购金关系的 id 是 `central_bank`/`central_bank_purchases` 中实际存在的那个。)

(e) `make_driver_section` 中,`driver-observation` 一格之后追加一格,表头补空列头:

```python
        <a class="evidence-link" role="cell" href="#{DRIVER_EVIDENCE_TARGETS[row['id']]}" data-evidence-target="{DRIVER_EVIDENCE_TARGETS[row['id']]}">证据 →</a>
```

(f) `build_html` 中 `make_research_details(dashboard)` 调用点改为 `make_research_section(dashboard)`。

- [ ] **Step 4: 旧断言映射改写(与实现同 commit,保持全绿)**

| 测试 | 旧断言 | 改为 |
|---|---|---|
| `test_html_uses_decision_first_structure` | `assertIn('<details class="research-evidence">')` 与 `assertNotIn('<details class="research-evidence" open>')` | `assertEqual(html.count('<details class="evidence-unit"'), 7)`;`assertNotIn('<details class="evidence-unit" open')` |
| `test_price_and_proxy_layers_remain_in_research_but_not_headline_score` | `assertIn("因子关系检验", html)` | `assertIn('id="evidence-auxiliary"', html)` 与 `assertIn("不参与首页姿态", html)` |
| `test_relationships_explain_factor_usefulness_by_phase` | `assertGreaterEqual(len(...["phases"]), 3)`(4 处)与 `assertIn("阶段表现", html)` | phases 断言整体替换为:对相应关系断言 `rolling_corr` 非空且 `latest_tone` 在三态词表内;`assertIn("关系演变", html)`。其余因子名/标签/「勿当稳定因果」断言保留 |
| `test_charts_show_axes_and_underlying_series` | (保留全部) | 追加:`assertIn('class="grid-line"', html)`、`assertIn("已反转", html)`、`assertIn('class="tone-band"', html)` |
| `test_research_details_include_scoring_method_and_limitations` | (文案全部保留于方法单元,预期不需改) | 若因估值行移动失败,估值断言改查辅助单元内 |

- [ ] **Step 5: 跑全量测试**

Run: `python3 -m unittest tests.test_build_site -v`
Expected: `OK`。若 `test_generated_html_has_no_trailing_whitespace` 失败,按其规则清理新模板字符串的行尾空白。

- [ ] **Step 6: 重建生成物并 commit**

```bash
python3 scripts/build_site.py
git add scripts/build_site.py tests/test_build_site.py site/index.html
git commit -m "feat: assemble evidence-chain research section with driver links

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH"
```

---

### Task 6: 退役清理 —— 阶段表体系、旧关系卡与三子块综合相关(死代码零残留)

**Files:**
- Modify: `scripts/build_site.py`(纯删除)
- Test: `tests/test_build_site.py`(追加一个防回归断言;其余 Task 5 已改写)

**Interfaces:**
- Consumes: Task 5 完成后的调用图(旧模板已无调用方)。
- Produces: 无新接口。`fmt_pct` 必须保留(仍被估值快照等使用)。

- [ ] **Step 1: 逐个确认零引用后删除以下函数(整个 def 块)**

对每个名字先跑 `grep -n "<名字>" scripts/build_site.py tests/test_build_site.py`,确认除定义外零引用再删;有引用则停下检查 Task 5 是否漏改,不得带引用强删:

1. `make_research_details`
2. `make_relationship_section`
3. `make_central_bank_relationship_card`
4. `make_horizon_relationship_card`
5. `make_positioning_relationship_card`
6. `weak_periods_from_rolling`(Step 2 删除各数据函数的 `weak_periods` 键后才会归零)
7. `corr_for_range`(Step 2 删除 phases 计算后才会归零)
8. `phase_label`、`phase_gold_return`(7-12 版保留 `phase_gold_return` 是因为段收益要用;分段撤回后按 grep 结果处理——仅 phases 体系引用则删,若 read 文案等仍引用则保留并在 commit message 注明)

- [ ] **Step 2: 删除数据函数中的阶段、弱窗口与综合相关残留**

(a) 在以下 5 个函数中,删除 `phase_defs` 列表、`phases` 循环计算、返回字典里的 `"phases"` 与 `"weak_periods"` 两个键(其余键与 read 文案不动):

- `make_monthly_factor_relationship`
- `make_central_bank_relationship`
- `make_two_horizon_relationship`
- `make_trend_relationship`
- `make_positioning_sub_metric`

(b) **`make_positioning_relationship` 中删除三子块综合相关**(E16 的代码层落实;≈1800 行):

```python
    latest_corrs = [item["latest_corr"] for item in sub_metrics if item["latest_corr"] is not None]
    latest_corr = sum(latest_corrs) / len(latest_corrs) if latest_corrs else None
```

这两行与返回字典中的 `"latest_corr"` 键一并删除;`"latest_tone": "分项观察"` 同样 grep 确认新代码无消费后删除。带符号相关的跨口径平均(GVZ 是 absolute)没有可解释含义,是 7-13 评审确认的缺陷,不得以任何形式保留。

(c) 在 `ResearchSectionTests` 追加防回归断言:

```python
    def test_positioning_relationship_has_no_composite_corr(self):
        dashboard = build_site.read_dashboard_data(today=self.TODAY)
        positioning = next(
            item for item in dashboard["relationships"]
            if item["id"] == "positioning_technical")
        self.assertNotIn("latest_corr", positioning)
```

- [ ] **Step 3: 全量测试保持绿**

Run: `python3 -m unittest tests.test_build_site tests.test_refresh_wind_data tests.test_update_data_and_commit -v`
Expected: `OK`(若有测试仍引用 `phases`/`weak_periods`/仓位 `latest_corr`,说明 Task 5 的映射改写有遗漏,回到该测试按映射表处理)。

- [ ] **Step 4: 重建并确认生成物零残留**

```bash
python3 scripts/build_site.py
grep -c 'phase-table\|metric-strip\|research-evidence\|relationship-card' site/index.html
```
Expected: `0`(CSS 里的旧类名残留在 Task 7 清理;此处 grep 的是 HTML 结构类名——若 CSS 命中,记下行号留给 Task 7,结构性命中必须为 0)。

- [ ] **Step 5: Commit**

```bash
git add scripts/build_site.py tests/test_build_site.py site/index.html
git commit -m "refactor: retire phase tables, legacy cards and composite corr

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH"
```

---

### Task 7: 内联 JS(仅溯源联动)与 CSS(新版式 + 旧类删除)

**Files:**
- Modify: `scripts/build_site.py`(`build_html` 模板:`<style>` 块与 `<script>` 块)
- Test: `tests/test_build_site.py`(新增 `ResearchInteractionScriptTests`)

**Interfaces:**
- Consumes: Task 5 输出的 `data-evidence-target` 锚链与 `.evidence-*` 类名。
- Produces: 页面级行为——点击驱动行「证据 →」展开并滚动到对应单元;无 JS 时纯锚点降级。**不含任何悬浮读数装置**(E14 暂缓)。

- [ ] **Step 1: 写失败测试**

```python
class ResearchInteractionScriptTests(unittest.TestCase):
    TODAY = date(2026, 7, 10)   # 与 ResearchSectionTests 一致

    def _html(self):
        return build_site.build_html(build_site.read_dashboard_data(today=self.TODAY))

    def test_html_embeds_evidence_link_script_without_hover_machinery(self):
        html = self._html()
        self.assertIn("link.dataset.evidenceTarget", html)
        self.assertIn('target.tagName === "DETAILS"', html)
        self.assertNotIn("data-hover-chart", html)   # E14 暂缓:图表与 JS 均无 hover 装置

    def test_styles_cap_evidence_body_and_hide_spark_chrome(self):
        html = self._html()
        self.assertIn(".evidence-body { max-width: 680px;", html)
        self.assertIn(".evidence-body svg { max-width: 520px;", html)
        self.assertIn(".evidence-spark svg text", html)
        self.assertNotIn(".phase-table", html)
        self.assertNotIn(".metric-strip", html)
        self.assertNotIn(".relationship-grid", html)
        self.assertNotIn(".research-evidence", html)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_build_site.ResearchInteractionScriptTests -v`
Expected: FAIL(脚本与样式尚未写入;旧 CSS 类仍在)。

- [ ] **Step 3: 写入 JS**

在 `build_html` 模板中现有 `activateRange("1y");\n})();` 的 IIFE 之后、`</script>` 之前追加一个 IIFE(与现有脚本同款防御风格;注意现有模板对 `{`/`}` 的转义惯例,照现有金价图脚本的写法处理花括号):

```javascript
(() => {
  document.querySelectorAll("[data-evidence-target]").forEach((link) => {
    link.addEventListener("click", () => {
      const target = document.getElementById(link.dataset.evidenceTarget);
      if (target && target.tagName === "DETAILS") target.open = true;
    });
  });
})();
```

- [ ] **Step 4: 写入新 CSS 并删除旧类**

(a) 在 `<style>` 块中(建议放在现 `.research-evidence` 规则的位置)新增:

```css
    .research-section { margin-top: 16px; }
    .evidence-unit { margin-top: 10px; scroll-margin-top: 12px; }
    .evidence-unit > .evidence-summary {
      display: flex; align-items: center; gap: 12px;
      padding: 12px 16px; cursor: pointer; list-style: none;
    }
    .evidence-unit > .evidence-summary::-webkit-details-marker { display: none; }
    .evidence-caret {
      width: 0; height: 0; border-left: 5px solid var(--muted);
      border-top: 4px solid transparent; border-bottom: 4px solid transparent;
      transition: transform 0.15s ease;
    }
    .evidence-unit[open] .evidence-caret { transform: rotate(90deg); }
    .evidence-title { min-width: 150px; display: flex; flex-direction: column; }
    .evidence-title small { color: var(--muted); font-size: 11px; }
    .evidence-spark svg { width: 64px; height: 16px; display: block; }
    .evidence-spark svg text, .evidence-spark svg line { display: none; }
    .evidence-corr { font-size: 13px; white-space: nowrap; }
    .evidence-count { font-size: 12.5px; color: #4a5568; white-space: nowrap; }
    .evidence-quality { margin-left: auto; font-size: 12px; }
    .evidence-unit[open] > .evidence-summary { border-bottom: 1px solid var(--line); }
    .evidence-body { max-width: 680px; padding: 4px 16px 16px; }
    .evidence-body svg { max-width: 520px; height: auto; display: block; }
    .evidence-hook {
      background: #fdf6e8; border: 1px solid #f0e3c4; border-radius: 6px;
      padding: 8px 12px; font-size: 13px; margin: 10px 0;
    }
    .evidence-hook a { margin-left: 6px; font-size: 12px; }
    .evidence-read { color: #334036; font-size: 13px; margin: 8px 0; }
    .evidence-chart-block h4 { margin: 14px 0 4px; font-size: 13px; color: #4a5568; }
    .evidence-sub-block { border-top: 1px dashed var(--line); padding-top: 10px; margin-top: 14px; }
    .evidence-sub-block h3 { font-size: 14px; display: flex; gap: 8px; align-items: center; }
    .evidence-sub-block h3 small { color: var(--muted); font-weight: normal; }
    .evidence-footnote { color: var(--muted); font-size: 11.5px; margin-top: 10px; }
    .evidence-link { font-size: 12px; white-space: nowrap; }
    .chart-box h5 { margin: 0 0 4px; font-size: 12.5px; color: #4a5568; }
    .grid-line { stroke: #ececec; stroke-width: 1; }
    .band-label { font-size: 10px; fill: #8a938c; }
```

媒体查询追加(并入现有对应断点块):

```css
    @media (max-width: 900px) {
      .evidence-body { padding: 4px 12px 12px; }
    }
    @media (max-width: 560px) {
      .evidence-spark { display: none; }
      .evidence-summary { flex-wrap: wrap; gap: 8px; }
    }
```

(b) 删除旧 CSS(按类名 grep 定位,整条规则删除;组合选择器只摘除对应项):

- `.relationship-grid`、`.relationship-card`、`.relationship-card-wide`、`.relationship-head`(含 ` h3`)、`.relationship-card h4`、`.relationship-read`
- `.metric-strip`(含 ` div`/` span`/` strong` 与 560px 断点内引用)
- `.phase-table`(两条 td 规则)
- `.research-evidence` 全部规则,并把共享面板选择器 `.price-panel, .technical-panel, .driver-panel, .recent-panel, .research-evidence {` 中的 `.research-evidence` 替换为 `.evidence-unit`(新单元复用同款白卡容器样式)
- `.dual-axis-grid`、`.dual-axis-item`(含其 h5 规则与两处媒体查询内引用;`.dual-axis-wrap`/`.dual-axis-legend`/`.corr-wrap`/`.corr-legend` 保留)

(c) 首屏驱动表加一列:找到 `.driver-head` / `.driver-row` 的 `grid-template-columns` 声明,在值末尾追加 ` 72px`(证据链接列);检查 ≤900px/≤560px 断点内若有同属性覆写,同样追加。

- [ ] **Step 5: 跑测试确认通过 + 重建**

Run: `python3 -m unittest tests.test_build_site -v` → `OK`
Run: `python3 scripts/build_site.py && grep -c 'phase-table\|metric-strip\|research-evidence\|relationship-grid' site/index.html` → `0`

- [ ] **Step 6: Commit**

```bash
git add scripts/build_site.py tests/test_build_site.py site/index.html
git commit -m "feat: add evidence deep-links and evidence unit styles

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01M9mxGax97ok1UNgf3G9QZH"
```

---

### Task 8: 全量验证与浏览器验收(不发布)

**Files:** 无代码改动;只运行验证与人工验收。

- [ ] **Step 1: 全量自动验证**

```bash
python3 -m unittest tests.test_build_site tests.test_refresh_wind_data tests.test_update_data_and_commit -v
python3 scripts/build_site.py
git diff --check
```
Expected: 全部 `OK`;`git diff --check` 无输出;重建后 `git status` 中 `site/index.html` 无新变化(可重复构建)。

- [ ] **Step 2: 结构抽查**

```bash
grep -c '<details class="evidence-unit"' site/index.html   # 期望 7
grep -c 'data-evidence-target' site/index.html              # 期望 ≥ 6(驱动行)+ JS 引用
grep -c 'class="evidence-hook"' site/index.html             # 期望 5(单元 1–5)
grep -c 'class="tone-band"' site/index.html                 # 期望 ≥ 30(10 张演变图 × 3–5 条带)
grep -c '已反转' site/index.html                             # 期望 ≥ 2(实际利率、美元)
grep -c 'data-hover-chart' site/index.html                  # 期望 0(E14 暂缓;金价主图用旧机制不受影响则另计)
```

(最后一条:首屏金价主图若本就使用 `data-hover-chart` 之外的自有机制则应为 0;若金价主图现有实现恰好用同名属性,以「研究区内为 0」为准,用 `sed -n` 截取研究区段落 grep。)

- [ ] **Step 3: 浏览器人工验收(`open site/index.html`)**

桌面(约 1440×900):
- 7 个单元默认全部收起;收起态单因子行可见 徽章 + sparkline + 滚动相关值,「资金与仓位」行可见计数文字(如「2 有效 · 1 失效」)且无 sparkline、无相关值;
- 点击首屏驱动行「证据 →」:对应单元展开并滚动就位(ETF 与 CFTC 都落在「资金与仓位」);
- 钩子条抽查(至少实际利率 + 资金与仓位):变化值与首屏驱动行一致、容忍带阈值数字与方法节一致、判定结论与首屏徽章一致;
- 实际利率/美元主图标注「已反转」且右轴刻度为原始数值(反转轴大值在下),购金/通胀/ETF/CFTC/GVZ 无反转标注;
- 演变图:三态阈值带可见、带内右缘态名可读、折线末点数值清晰;
- **专项复核(决定是否再议分段)**:顺折线读 2–3 个历史区间,判断「哪段有效哪段失效」是否无需辅助即可读出;若明显难读,记录截图与理由,报告用户后再议(不擅自加回分段);
- 展开态图表宽 ≤520px、内容列 ≤680px,无图文失衡;标准单元展开总高约 ≤1.5 屏;
- 控制台无错误。

移动(DevTools 390×844):
- summary 行 sparkline 隐藏,徽章/计数与数值仍在;无横向溢出。

无 JS(浏览器禁用 JavaScript 后刷新):
- 7 个 `<details>` 原生可开合;全部 SVG 静态完整(线、阈值带、刻度、末点数值);
- 「证据 →」退化为纯锚点:跳到收起单元的 summary 行;
- 页面无空白区块、无脚本错误痕迹。

- [ ] **Step 4: 收尾确认(不发布)**

全部通过后停在本地工作区;向用户报告验收结果(含分段专项复核结论)。`git push` 与 GitHub Pages 部署需用户单独确认(项目红线)。

---

## 计划自审记录(spec 覆盖 / 类型一致 / 7-13 修订对照)

**Spec 覆盖对照:** E1/E2→Task 5;E3(不做归因/热图)→无对应任务(正确);E4→Task 2+4(`invert_factor` 唯一判定点在 `make_evidence_relationship_block`);E5/E6→Task 1(`corr_tone_bands` 有探针测试锁定与 `corr_tone` 的一致性);E7→Task 4/5(日频单元 corr_label 用中期窗、短期线经 `short_term` 以虚线叠加);E8→`corr_tone`/`tone_badge` 全程复用;E9→Task 4 尾注「重叠样本」+ Task 5 方法节新 li;E10→Task 4 sparkline 画 `rolling_corr`(仅单因子);E11→新结构无 metric-strip、钩子在 body 首位;E12→Task 7 CSS;E13→未用 `<details name>`;E14(暂缓)→全计划无 hover 装置,且 Task 2/7 有 `assertNotIn("data-hover-chart")` 反向锁定;E15→Task 3(阈值来自常量、missing 分支、判别性断言);E16→Task 5 `tone_counts_text` + Task 6 删除综合相关 + 防回归断言。spec §11 测试合同 1–9 分别落在 Task 1/3/5/5/5+6/2+5/5/4/5–6。

**7-13 评审三问题的落实点:**
1. 分段悖论 → 计划中不存在 `segment_rolling_corr`(Global Constraints 明令),演变图合同 = Task 1 阈值带;
2. 复合混口径 → Task 5 计数 summary(`test_positioning_summary_shows_tone_counts_not_average`)+ Task 6 删平均(`test_positioning_relationship_has_no_composite_corr`);
3. 钩子空洞 → Task 3 判定推导 + missing 分支,断言绑定阈值数字与判定词,非「首屏当前」字样烟测。

**已知折中(实现者须知):**
1. Task 5 的 `make_method_evidence_unit`/`make_auxiliary_evidence_unit` 以「平移」指令给出:源代码是 `make_research_details` 内的既有计算(实现时以 grep 定位),不是凭空占位——既有文案字符串逐字保留是 `test_research_details_include_scoring_method_and_limitations` 保绿的前提。
2. `make_dual_axis_chart` 左右轴含义对调(金价移到左轴):旧调用点全部随 Task 5 重组消失,无兼容层。
3. Task 3 测试用合成 layer 字典驱动,不读 Excel;「未计入姿态」文案与 `score_driver_layers` 剔除行为的一致性靠共享同一组条件常量保证,不重复实现投票逻辑。
4. 阈值带边界点(如 corr 恰为 +0.35)在渲染上属于相邻两带的共享像素边界,无歧义;判定归属由 `corr_tone` 的 ≥/≤ 定义,探针测试允许边界点命中任一相邻带。

**类型一致:** `corr_tone_bands` 输出 `(float, float, str)` 三元组(Task 1 定义与测试消费);钩子函数只读 layer 既有键(`change_label`/`lag_days`/`sub_states`/`latest`,已对照 `make_*_layer` 与 `read_dashboard_data` 核实存在);`make_evidence_unit` 参数序(unit_id, title, source_note, relationship, layer, chart_limit, spark_limit, corr_label)在 Task 4 定义、Task 5 消费,已核对一致。
