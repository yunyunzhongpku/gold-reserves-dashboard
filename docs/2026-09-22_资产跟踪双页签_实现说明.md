# 资产跟踪双页签实现说明

日期：2026-09-22。范围：将现有黄金看板与境内美股ETF看板合为一个网页的“黄金 / 美股 ETF”页签。本轮不调整指标、计算规则、ETF名单或数据源。

## 页面与输入

`scripts/build_site.py` 继续按原函数生成完整黄金页，再调用 `merge_assets.write_merged_site` 与 `data/etf_tracking.html` 合并。后者是ETF项目经过校验的自包含成品快照，不携带凭据，也不在网页中请求Wind或Tushare。

`site/index.html` 为自包含成品：两页的样式与脚本分别保留，统一顶部页签。首次打开默认黄金；`#gold`、`#us-etf` 可直接进入相应页。切换时保留内部筛选与图表状态，返回页面顶部。支持方向键、Home、End。黄金证据链接在本页展开，不跳转重载整份合并页。

未修改黄金 `build_html` 内部计算及页面内容；黄金原有指标、观测日期、滞后状态和已知局限继续展示。两边数据日期独立，不以统一构建时间代替数据日期。离线文件是当时的快照；只有部署到共享网址并更新后，其他人刷新网址才能取得新版。

## 更新方式

首次导入或只在本地合并已有ETF页面：

```sh
python3 scripts/build_site.py --etf-page '/Users/xu/code/ETF研究/QDII ETF 溢价率/site/美股ETF跟踪.html'
```

之后普通 `python3 scripts/build_site.py` 始终读取保存的ETF快照。因此现有GitHub Actions再次构建，以及黄金周更，都不会丢失ETF页签。CI仍只渲染已提交数据；本轮不改CI配置。

黄金完整周更入口保持 `scripts/update_data_and_commit.py`，仍刷新原Wind/FRED/CFTC数据。ETF项目先完成原有日更后，可用同一发布入口的ETF模式：

```sh
python3 scripts/update_data_and_commit.py --etf-page '/Users/xu/code/ETF研究/QDII ETF 溢价率/site/美股ETF跟踪.html'
```

此模式只导入ETF快照、重建合并页与运行检查，不查询黄金数据。只允许提交 `data/etf_tracking.html`、`site/index.html` 两项；若出现黄金市场CSV等其他tracked变更则停止。该命令默认会commit、push，必须在发布授权后使用；仅构建预览请使用前一个命令。

输入缺失、JSON损坏、未来日期、名单数量/分组或代码唯一性不符时，合并停止，不退回黄金单页。两项输出先准备完整再替换，写入失败恢复旧缓存与旧成品。目录中的未跟踪研究文件不纳入提交。

## 联合日更

用户于2026-09-22确认公开发布，并指定黄金和ETF统一每天北京时间20:30更新，包括周末和节假日。沿用原黄金自动化标识 `commit`、本机项目环境及GPT-5.6 Sol high，改名为“资产跟踪 每日更新与发布”；原独立ETF自动化 `etf` 暂停，不删除历史，也不新建重复任务。

每天同一轮先在ETF项目运行 `python3 更新美股ETF跟踪.py`，依据交易日历读取最近完整交易日及真实已披露数据；随后在黄金仓库运行：

```sh
python3 scripts/update_data_and_commit.py --etf-page '/Users/xu/code/ETF研究/QDII ETF 溢价率/site/美股ETF跟踪.html' --refresh-gold
```

联合模式先刷新黄金原Wind/FRED/CFTC数据，再导入已校验的ETF快照，统一构建、检查、commit、push和Pages核验。只允许原黄金市场数据、ETF缓存和合并成品进入数据提交。任何一边失败就停止本轮发布，保留现有线上版本，标明失败和未更新范围；不伪造新日期，不绕过对账或测试。非交易日仍按数据源真实披露情况检查；不会要求每个源每天都有新观测。电脑、Codex及现有数据连接需要在更新时可用。

公开网址：https://yunyunzhongpku.github.io/gold-reserves-dashboard/ 。访问者无需Wind、Tushare或本机服务；页面包含静态数据。初次发布使用已验收的2026-09-21行情快照，首轮联合日更从2026-09-22 20:30开始。

## 验证与发布记录

- 全量 `unittest discover -s tests` 已通过133项；其中新增16项覆盖HTML完整保留/转义、页签结构、ETF输入边界、两文件失败恢复、两种更新模式及ETF提交范围。
- 主代理在所有Sol返回后做浏览器验收，额外修复了srcdoc内锚点导航、内容收缩后的高度以及切换页签时的焦点/滚动位置。
- 桌面与390像素手机宽度已实看；两页签、黄金证据展开/收起、ETF分组和订单筛选、切页保留状态、键盘方向键/Home/End、直接打开ETF页签均已核对。最终布局修订后新增16项检查再次通过。
- 从成品反解析的ETF内页与原始输入完全一致；黄金内页与原函数重新生成结果仅生成时间不同，按同一生成时间核对后完全一致。本地预览与合并成品一致；黄金原工作区20个受保护文件指纹保持不变，原有未跟踪研究文件未纳入本轮。
- 开发在 `codex/gold-etf-tabs` 隔离分支，本地预览文件位于ETF项目的 `site/资产跟踪.html`。用户确认前未改线上页面或现有自动化；确认后按上述联合日更方案发布并调整原任务。
- 联合刷新另增4项行为测试，覆盖取数顺序、联合白名单、参数约束以及ETF单独更新的原有边界；由Sol编写、主代理独立运行验收。发布时还须核对远端commit、对应Pages工作流和公开页面中的两个内页。
- 联合更新入口完成后，全量137项测试通过；既有任务已改为每日20:30，原ETF独立任务已暂停。
